# Middle of Math 실행 계획

## Interview Summary

현재 3학년부터 6학년 2학기까지의 진단 콘텐츠와 학생·교사·콘텐츠 스튜디오의 기본 흐름이 구현되어 있다. 다음 단계는 콘텐츠 양을 늘리는 것이 아니라 실제 교실에서 교사용 분석지가 다음 수업 결정에 도움이 되는지 검증하는 것이다.

## Confirmed Decisions

- 다음 4–6주의 최우선 목표는 실제 교실 파일럿 운영이다.
- 첫 운영자는 서비스 제작자인 교사 본인이다.
- 첫 파일럿 대상은 본인이 담당하는 한 학급이다.
- 파일럿 콘텐츠 범위는 초등학교 3학년 2학기 6개 단원 전체다.
- 가장 중요한 성공 기준은 교사용 분석지가 다음 수업 계획에 실제로 도움이 되는가이다.
- 대상 학급은 학생 25명이며, 학생은 각자 태블릿으로 참여한다.
- 파일럿은 6주 동안 매주 한 단원씩 운영한다.
- 한 단원 진단 시간은 15분으로 제한한다.
- 각 단원 종료 후 교사는 분석지를 읽고 후속 행동 하나와 선택 이유 한 문장을 기록한다.
- 후속 행동은 `전체 수업에서 다시 설명`, `소집단 확인 활동`, `학생 한 명에게 추가 질문`, `별도 조치 없음` 중에서 선택한다.
- 파일럿 성공 기준은 다음과 같다.
  - 6개 단원 중 4개 이상에서 분석지가 구체적인 다음 수업 행동을 만들어낸다.
  - 학생의 80% 이상이 각 단원 활동을 끝까지 완료한다.
  - 학생 평균 완료 시간이 20분 이내다.
  - 교사의 실제 관찰과 분석지의 핵심 내용이 70% 이상 일치한다.
  - 학생 데이터 손실이나 진행 불가 오류가 0건이다.
- 파일럿 운영 데이터의 원장은 Supabase로 확정한다.
- 기존에 사용 중인 Neon은 이번 파일럿에 연결하지 않는다. 파일럿이 끝난 뒤 비용·운영성 비교 후보로만 검토한다.
- 분석지 일치도는 매주 분석지를 열기 전에 교사가 학생 5명의 예상 어려움을 먼저 기록한 뒤 결과와 비교한다.
- 6주 동안 확인 학생을 바꾸어 25명 모두 최소 한 번 포함한다.
- 학교 관리자 확인과 보호자 안내를 마친 뒤 실제 학생 데이터를 수집한다.
- 아래 상황에서는 해당 주 파일럿을 즉시 중단하고 원인을 수정한다.
  - 시작 10분 안에 5명 이상 로그인하지 못한다.
  - 완료한 학생의 응답 기록이 하나라도 사라진다.
  - 같은 문장이나 화면을 3명 이상 이해하지 못한다.
  - 학생 화면에 정답 또는 학생 진단 단서가 노출된다.
  - 교사용 분석지가 2명 이상 생성되지 않는다.
- 학생에게는 진단명이나 오개념 이름을 직접 보여주지 않는다.
- 한 문항의 오답만으로 학습 결손이나 오개념을 확정하지 않는다.
- `grade3-semester2@2.1.0` 발행 검토는 파일럿 운영자 본인이 수행한다. 다른 7개 학기와 같은 콘텐츠 검수 체크리스트와 독립 검증 에이전트 승인을 발행 조건으로 유지하되, 별도 두 번째 교사 검토는 요구하지 않는다(2026-08-05 확정, addresses: 두 번째 검수 교사를 구할 수 없음).

## Education Brief

- 학습자: 초등학교 3학년 학생
- 운영자: 담임 또는 수학 수업을 담당하는 교사 본인
- 학습 목표: 3학년 2학기 수학의 단원별 작은 학습 단계에서 학생의 생각이 흔들리는 지점을 확인한다.
- 상호작용: 학생은 짧은 진단 문항에 답하고, 시스템은 선택·풀이 시간·선택 변경·잘 모르겠어요 사용 기록을 수집한다.
- 진단 결과: 학생에게 정오답 진단을 직접 노출하지 않고, 교사가 문항별 응답과 여러 문항에서 반복된 생각을 확인한다.
- 교사 활용: 분석지를 읽고 다음 수업의 전체 지도, 소집단 확인 또는 개별 질문을 결정한다.
- 개인정보 원칙: 실명과 학교 식별 정보는 수집하지 않고 번호와 선택 별칭을 사용한다.

## Assumptions

- 파일럿은 정규 수업 또는 수업에 인접한 시간에 교사가 직접 감독한다.
- 학생 25명이 동시에 사용할 수 있는 태블릿과 네트워크가 준비되어 있다고 가정한다.
- 한 번에 3학년 2학기 전체 문항을 풀게 하지 않고 단원별 활동으로 나눈다.
- 파일럿 중에는 자동 생성 결과를 확정 진단으로 사용하지 않고 교사의 관찰과 비교한다.
- 첫 파일럿에서는 AI 분석을 추가하지 않고 현재 규칙 기반 분석 결과를 검증한다.
- 교사의 파일럿 관찰 기록은 별도 Google Form과 Sheet로 먼저 운영한다. 서비스 안의 기록 기능은 파일럿에서 필요성이 확인된 뒤 구현한다.

## Open Questions

- 태블릿 25대가 학교 네트워크에서 동시에 서비스에 접속할 수 있는가?
- 학생별 개인 코드 25개를 어떤 방식으로 배부하고 분실·재발급을 처리할 것인가?
- 학교 관리자 확인과 보호자 안내에 사용할 실제 문서는 누가 작성·승인할 것인가?
- production Supabase 프로젝트의 리전과 유료 백업 수준은 무엇으로 정할 것인가?

## Architecture Decisions

### Data model

- `core`: 교사, 학급, 학생 번호·별칭, 학생 접근 권한, 진단 발행본, 배정, 학생 활동, 문항별 응답 기록, 분석 실행, 교사용 분석지, 가정 공유용 결과표를 Supabase Postgres의 실제 데이터로 유지한다.
- `core`: 학생 응답 기록은 추가만 가능한 원자료로 저장하고, 분석 결과는 원자료를 바꾸지 않는 별도 실행 결과로 저장한다.
- `core`: 학생 실명·학교 식별 정보는 저장하지 않는다.
- `scaffold`: 교사의 파일럿 관찰과 후속 수업 행동은 Google Form/Sheet에 익명 학생 번호로 기록한다.

### Module seams

- `core`: 학생·교사·스튜디오 화면은 application 계층의 use case와 repository 인터페이스만 사용한다.
- `core`: Supabase 인증·조회·저장·RPC는 adapters 패키지 안에 둔다.
- `core`: 교육 콘텐츠는 packages/content의 검토·발행된 버전을 원본으로 사용한다.
- `scaffold`: 향후 Neon 비교가 필요하면 repository adapter를 새로 만들되 UI와 domain 규칙은 바꾸지 않는다.

### Real vs stub

- `core`: Supabase Auth, Postgres, RLS, RPC, retention purge, staging·production 배포는 실제 운영 경로로 완성한다.
- `core`: 3학년 2학기 2.1.0의 6개 단원은 실제 검토·발행본으로 운영한다.
- `scaffold`: 교사 연구 기록은 외부 Form/Sheet로 운영한다.
- `scaffold`: AI 요약, 자동 처방, 보호자 계정, 학교 관리자 대시보드는 파일럿에서 구현하지 않는다.

### Extension points

- 파일럿 뒤 교사 관찰 기록을 서비스 내부 분석지에 연결할 수 있다.
- 파일럿 뒤 초대 교사 1–3명으로 운영 범위를 확장할 수 있다.
- 파일럿 결과가 유효할 때 다른 학년 진단을 같은 발행·배정·분석 계약에 연결한다.

### State and source of truth

- 학생·교사 운영 데이터의 단일 원장은 production Supabase다.
- 콘텐츠 원본은 Git 저장소이고, 학생이 푸는 버전은 Supabase에 발행된 불변 JSON이다.
- 학생 태블릿은 오프라인 큐를 임시 상태로 사용하고 동기화가 끝난 서버 기록을 최종 상태로 본다.
- 교사 연구 기록의 임시 원장은 Google Sheet이며 파일럿 종료 뒤 익명 집계만 제품 결정에 사용한다.

## Pilot Observation Protocol

1. 단원 진단 전에 그 주의 확인 학생 5명을 정한다.
2. 분석지를 열기 전에 각 학생에게 예상되는 어려움 또는 `특이사항 없음`을 한 줄로 기록한다.
3. 학생은 15분 동안 단원 진단을 수행한다.
4. 교사는 완료율·평균 시간·기술 오류를 확인한다.
5. 분석지의 가장 먼저 살펴볼 내용과 사전 관찰을 비교한다.
6. 일치·부분 일치·불일치 중 하나를 선택하고 이유를 한 문장으로 남긴다.
7. 후속 행동 하나를 선택하고 실제 다음 수업에서 실행한다.
8. 다음 주에 후속 행동이 도움이 됐는지 `도움 됨·판단 어려움·도움 안 됨`으로 기록한다.

## Implementation Backlog

### P0 — 파일럿을 막는 항목

1. [완료] smoke 선택자 계약과 legacy adapter 기대 문구를 현재 용어에 맞춰 CI를 복구한다.
2. [완료, 2026-08-05] staging의 Supabase access token·DB password·smoke 교사 계정을 등록한다. `Release staging`의 `migrate` 잡이 실제 staging DB에 마이그레이션 체인을 적용하는 데까지 성공했다(런 30986769763).
3. [완료, 2026-08-06] staging-retention에 service-role key를 등록하고 삭제 작업을 검증했다. production-retention은 production Supabase 프로젝트가 없어 삭제 매트릭스에서 제외했다(`0c84c13`, 결함이 아니라 의도된 축소).
4. [검수 완료, 2026-08-07] 3학년 2학기 64문항의 발행 경로를 열고(`202608060005`, 2.1.0) 내용 검수를 마쳤다. 발행 SQL 생성기가 grade4-semester1 전용이라 나머지 7개 학기가 전부 발행되지 못하고 있었고, 이를 `generatePublicationSql`로 일반화해 해소했다. 검수는 파일럿 운영자 본인과 검증 에이전트 둘(codex, gemini)이 각각 64문항 전체를 봤고, 확정된 결함 13건을 반영한 2.2.0을 새로 발행했다(`202608070001`, checksum `5b8648f0…52713`). 2.2.0 재검수는 64/64 통과·수정 필요 0건이다. 별도 두 번째 교사 검토는 요구하지 않는다.
5. [완료] 교사가 학기와 단원을 따로 선택해 배정하는 migration — CI pgTAP(런 30981938079)과 staging DB 실제 적용(런 30986769763)을 모두 통과했다.
6. [완료, 2026-08-05] 무효화됐던 `VERCEL_TOKEN`을 재발급했다. teacher·studio·student 세 배포가 모두 성공했다(런 30986769763 rerun). 재배포 뒤 세 프로젝트의 Vercel Deployment Protection(SSO, `all_except_custom_domains`)이 커스텀 도메인이 없는 staging별칭 전체를 로그인 화면으로 막고 있는 것을 추가로 발견해 사용자 확인 후 껐다. 세 URL 모두 curl로 200 확인, studio 로그인 화면 smoke 테스트 통과.
7. [완료, 2026-08-06] 교사 계정 생성 경로를 복구했다. `create_teacher_profile()`이 INSERT 시점에 `invited_at`을 요구해 GoTrue의 2단계 초대(INSERT 후 `invited_at` UPDATE)를 막고 있었다 — Dashboard의 `Create new user`와 `Invite user`가 모두 같은 예외로 실패했고, 손수 INSERT한 우회 행은 GoTrue 밖에 있어 로그인·수정·삭제가 되지 않았다. `202608060001_teacher_invite_two_phase.sql`로 프로필 생성 시점을 초대 기록 시점으로 옮기고, pgTAP에 "초대 전 프로필 없음 → 초대 후 프로필 생성" 두 단계를 검증으로 추가했다. staging에서 계정을 정상 경로로 재생성해 `auth.users` 1행·`public.teachers` 1행을 확인했다.
8. [완료, 2026-08-06] staging 전체 흐름 검증. 교사 로그인 → 학급 생성 → 학생 추가 → 단원 배정 → 학생 완료 → 반 요약 → 학부모 결과표 인쇄까지 실제 staging에서 통과했고, `parent_report_exports`에 실제 행이 남는 것도 확인했다. 이 흐름은 아래 결함들 때문에 그동안 한 번도 끝까지 성공한 적이 없었다.
   - Vercel Preview 환경변수가 teacher만 갱신돼 있고 student·studio는 로컬 Supabase 주소였다. 세 앱을 staging 값으로 통일하고, 배포 전에 pull한 `VITE_SUPABASE_URL`이 비어 있거나 로컬이면 중단하는 가드를 두었다.
   - `create_student`가 출력 컬럼 `active`와 `classes.active`의 이름 충돌로 호출될 때마다 42702를 일으켜 학생 추가가 항상 실패했다(`202608060002`). pgTAP이 이 함수를 한 번도 호출하지 않아 드러나지 않았고, 이번에 성공·타반 거부 두 검증을 추가했다.
   - 배정 마법사의 `다음` 버튼이 모든 단계에서 진단·단원까지 요구해, 학급 학년과 다른 진단이 기본 선택되면 2단계로 넘어갈 수 없는 교착이 생겼다. 단계별로 필요한 조건만 검사하도록 바꿨다.
   - staging Supabase의 익명 로그인이 꺼져 있어 학생 입장이 전부 거부됐다. `external_anonymous_users_enabled`를 켰다. 이 값은 학생 입장의 전제이므로 production 신설 시에도 확인해야 한다.
   - 학부모 결과표 저장이 42501로 거부됐다. `guard_parent_report_export_consistency`가 교사 권한으로 실행되는데 2026-08-02 콘텐츠 마이그레이션들이 `jsonb_object_has_only_keys`의 실행 권한을 `authenticated`에서 회수했기 때문이다. 그날 이후 어떤 교사도 결과표를 내보낼 수 없었다. 가드를 `security definer`로 바꿔(`202608060003`) 헬퍼는 클라이언트에서 계속 막힌 채로 두었다.

### P1 — 첫 수업 전에 필요한 항목

0. **2.2.0 이외의 `grade3-semester2` 발행본을 retire 한다.** 2026-08-07 현재 staging 에는 `1.0.0`·`2.1.0`·`2.2.0` 세 행이 모두 `published` 다. 교사 배정 목록은 `ContentStudioRepository.listPublished`가 `published_at` 내림차순으로 돌려주는 것을 그대로 보여 주며 최신 버전을 자동 선택하지 않는다. 따라서 교사가 `2.1.0`을 고르면 내용 검수에서 확인한 정답 노출 문항(`g3s2-div-04`, `g3s2-div-06`)이 그대로 학생에게 나간다. 게다가 `1.0.0`의 `published_at`은 마이그레이션 적용 시각이라 `2.2.0`의 고정값(`2026-08-07T00:00:00Z`)보다 뒤일 수 있고, 로컬 재현에서는 실제로 12문항짜리 `1.0.0`이 목록 맨 위에 떴다. `public.retire_diagnosis_set(uuid)`가 있고 불변 트리거도 `retired` 전환은 허용한다. `1.0.0`은 기존 배정이 걸려 있을 수 있으므로 `assignments` 참조를 먼저 확인한다.
1. 태블릿 25대 동시 접속과 학교 Wi-Fi에서 student URL 접근을 확인한다.
2. 학생 번호별 개인 코드 카드를 만들고 분실·재발급 절차를 리허설한다.
3. [초안 완료] Google Form/Sheet 기반 파일럿 관찰 기록지를 만든다.
4. [초안 완료] 관리자 확인 및 보호자 안내 문구를 준비한다.
5. production Supabase를 staging과 분리해 만들고 검증된 커밋만 수동 승격한다.
6. 첫 단원 전에 교사 1명·학생 역할 기기 3대로 15분 리허설을 한다.

### P2 — 파일럿 중 개선

1. 동일한 학생 질문이 3회 이상 반복되면 문구·화면 결함으로 등록한다.
2. 분석지와 교사 관찰의 불일치가 반복되면 문제 추가보다 분석 규칙과 오답 근거를 먼저 검토한다.
3. 한 단원 평균 완료 시간이 20분을 넘으면 해당 단원의 문항 수 또는 상호작용 부담을 줄인다.

## Six-Week Schedule

| 주 | 단원 | 운영 후 필수 기록 |
|---|---|---|
| 1주 | 곱셈 | 로그인 성공률, 완료율, 시간, 5명 사전 관찰, 후속 행동 |
| 2주 | 나눗셈 | 같은 항목 + 1주 후속 행동 효과 |
| 3주 | 원 | 같은 항목 + 도형 화면 이해 문제 |
| 4주 | 분수 | 같은 항목 + 분수 표기·조작 이해 문제 |
| 5주 | 들이와 무게 | 같은 항목 + 단위·생활 맥락 이해 문제 |
| 6주 | 그림그래프 | 같은 항목 + 범례·그래프 읽기 이해 문제 |

## Final Decision Rubric

- `계속 개발`: 핵심 성공 기준 4개 이상 충족하고 분석지가 4개 단원 이상에서 후속 수업 행동을 만든다.
- `수정 후 재파일럿`: 데이터 손실은 없지만 분석 일치율·완료 시간·문구 이해 중 하나가 기준 미달이다.
- `파일럿 중단`: 데이터 손실, 권한 침해, 학생 진단 단서 노출 또는 분석지 대량 생성 실패가 발생한다.
- 첫 파일럿 결과만으로 학생 진단의 타당성을 확정하거나 다른 학년으로 자동 확대하지 않는다.

## Change Log

- 추가 콘텐츠 제작 우선 -> 한 학급 6주 파일럿 우선 (addresses: 실제 수업 가치 미검증)
- 전 학년 동시 운영 -> 3학년 2학기 6개 단원 운영 (addresses: 범위 과다)
- 분석지 열람 뒤 인상 기록 -> 열람 전 5명 사전 관찰 후 비교 (addresses: 확인 편향)
- 서비스 내부 연구 기록 구현 -> Google Form/Sheet로 먼저 검증 (addresses: 파일럿 전 과도한 구현)
- 기존 Neon 재사용 검토 -> 파일럿은 현재 구조의 Supabase 사용 (addresses: 인프라 재개발과 일정 지연)
- 정상 운영을 전제로 시작 -> 명시적 주간 중단 기준 적용 (addresses: 학생 피로·데이터 손실·기술 장애)
- 2.1.0 발행에 다른 교사 검토 요구 -> 파일럿 운영자 본인의 콘텐츠 검수 체크리스트·독립 검증 에이전트 승인으로 검토 (addresses: 두 번째 검수 교사를 구할 수 없음; 이미 발행된 다른 7개 학기도 같은 절차로 검토됐다)

## Current Readiness Findings (2026-08-05 갱신)

- staging Supabase 프로젝트가 오늘 재활성화됐다. `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `SUPABASE_PROJECT_REF`, `STAGING_TEACHER_EMAIL`, `STAGING_TEACHER_PASSWORD`가 staging 환경에 등록됐고, `Release staging`의 `migrate` 잡이 실제 staging DB에 마이그레이션 체인을 적용하는 데 성공했다(런 30986769763). 이전에 기록했던 "비활성 상태·비밀값 미등록" 문제는 해소됐다.
- 단원 배정 migration의 pgTAP 검증이 CI(런 30981938079, "Verify migrations and pgTAP policies")에서 통과했다. "코드 완료·DB 실행 대기" 상태는 해소됐다.
- `VERCEL_TOKEN`을 재발급해 teacher·studio·student 세 앱 배포가 모두 성공했다(런 30986769763). 이어서 세 프로젝트의 Vercel Deployment Protection(SSO)이 `all_except_custom_domains`로 켜져 있어 커스텀 도메인이 없는 staging 별칭이 전부 Vercel 로그인 화면으로 리다이렉트되던 것을 발견했다 — 이 상태로는 학생·교사도 태블릿에서 접속할 수 없었다. 사용자 확인 뒤 세 프로젝트의 SSO 보호를 껐고, 세 URL 모두 200 응답과 studio 로그인 화면 smoke 통과를 확인했다.
- 교사 계정을 어떤 경로로도 만들 수 없던 원인을 찾아 고쳤다(2026-08-06). `create_teacher_profile()`이 `auth.users` INSERT 시점에 `invited_at`이 비어 있으면 예외를 던졌는데, GoTrue의 관리자 초대는 행을 먼저 INSERT하고 `invited_at`을 나중에 UPDATE하는 2단계다. 그래서 Dashboard의 `Create new user`뿐 아니라 문서가 유일한 경로로 지정한 `Invite user`까지 같은 예외로 막혔고, 우회로 손수 INSERT한 행은 GoTrue가 관리하지 않아 로그인·수정·삭제가 모두 실패했다. `202608060001_teacher_invite_two_phase.sql`이 프로필 생성 시점을 초대가 기록되는 순간으로 옮겨 두 경로를 모두 살렸다. "초대 없는 계정은 교사 프로필을 얻지 못한다"는 원래 불변식은 그대로다.
- staging에 위 수정을 적용하고 계정을 정상 경로로 재생성해 `auth.users` 1행·`public.teachers` 1행을 확인했다.
- 계정 문제가 풀린 뒤 전체 흐름을 실제 staging에서 돌려 결함 5개를 더 찾아 고쳤다: Vercel Preview 환경변수가 세 앱 중 하나만 갱신돼 있던 것, `create_student`의 `active` 이름 충돌, 배정 마법사의 단계 교착, staging의 익명 로그인 비활성화, 학부모 결과표 가드의 실행 권한. 이제 교사 로그인부터 학부모 결과표 인쇄까지 전 구간이 통과한다.
- 이 결함들은 모두 실사용 경로에서만 드러났고, 단위·pgTAP·데모 모드 검사는 전부 통과하고 있었다. 파일럿 전 실제 기기로 한 번 끝까지 밟아보는 리허설을 생략하지 않는다.
- `staging-retention`에 `SUPABASE_SERVICE_ROLE_KEY`를 등록하고 `Purge expired pilots`를 수동 실행해 검증했다(런 31097926580). 첫 실행은 `curl: (7) Failed to connect to 127.0.0.1 port 54321`로 다시 실패했는데, 이 환경의 `SUPABASE_URL`이 2026-07-22 등록 이후 갱신된 적 없이 로컬 Supabase 주소를 가리키고 있었기 때문이다 — `staging` 환경과 별개로 등록된 시크릿이라 앞서 고친 "로컬 URL로 빌드된 배포" 결함(`d29500d`, `5a4d650`)과 같은 종류가 이 환경에도 남아 있었다. 실제 staging 프로젝트 URL(`https://wuptdjktawxzdvuenefd.supabase.co`)로 교체한 뒤 재실행해 정상 응답을 확인했다(`{"events": 0, ...}` 전부 0 — 현재 삭제 대상 없음, 연결과 RPC 실행 자체가 정상임을 확인). 야간 스케줄(18:17 UTC)로 계속 검증된다.
- `production-retention`은 오늘 커밋(`0c84c13`)에서 삭제 매트릭스 대상에서 제외됐다. production Supabase 프로젝트가 아직 없기 때문이며, 프로젝트 신설 자체가 여전히 미착수 상태다.
- 6개 단원·64문항인 `grade3-semester2@2.1.0`을 staging에 발행했다(2026-08-06). 그동안 배정 가능한 운영본이 12문항 `1.0.0`뿐이었던 것은 콘텐츠가 없어서가 아니라 발행 경로가 없었기 때문이다. `tools/emit-published-set-sql.ts`가 grade4-semester1 전용으로 하드코딩돼 있어 나머지 7개 학기(3-1, 3-2, 4-2, 5-1, 5-2, 6-1, 6-2)는 작성만 되고 `diagnosis_sets`에 닿을 방법이 없었다. 재사용 가능한 절반을 `generatePublicationSql`로 분리했고, 개수만 맞고 짝이 어긋나는 오답 근거를 DB 트리거 이전에 잡도록 검증을 추가했다.
- 콘텐츠 자체는 TS 소스의 `status: "review"`가 아니라 마이그레이션이 발행 여부를 정한다. grade4-semester1도 소스에서는 `review`인 채로 DB에는 `published`로 들어가 있다. "2.1.0이 review 상태라 못 쓴다"는 이전 기록은 원인을 잘못 짚은 것이었다.
- 발행하면서 기준 발행본이 배포 경로에 닿은 적이 없다는 것을 발견했다. `grade3-semester2@1.0.0`이 `supabase/seed.sql`에만 있었는데, seed는 `supabase start`에서만 실행되고 `Release staging`이 쓰는 `supabase db push`에서는 실행되지 않는다. **P1-5로 예정된 production Supabase를 새로 만들었다면 교사가 배정할 콘텐츠가 하나도 없는 상태로 올라왔을 것이다.** 또 seed가 모든 마이그레이션 뒤에 돌기 때문에 2.1.0 발행 직후 seed의 1.0.0 삽입이 안정 ID 보존 트리거에 걸려 CI가 깨졌다(런 31102929564). seed 문장을 그대로 마이그레이션 체인(`202608060004`)으로 옮겨 두 문제를 함께 없앴다.
- 2.1.0 발행은 DB 트리거가 검사하는 조건을 사전에 TS로 전부 확인한 뒤 올렸다: 1.0.0의 단원·성취기준·학습단계·신호·문항 안정 ID 소실 0건, 학생 노출 금지 문구 위반 0건, 미정의 signalId 참조 0건, prerequisite 순환 없음, 오답 근거 128/128. DB checksum도 TS의 `canonicalJson`+SHA-256으로 재현되는 것을 grade4-semester1의 기존 핀 값으로 대조 확인한 뒤 pgTAP에 핀으로 박았다(`566762c8…f623ae`).
- 상위 학습지도 스냅샷(`DECK6/korean-elementary-learning-map` @ `3ef0563`) 기준으로 성취기준 커버리지를 실측했다. 5–6학년군은 45개 중 45개, 3–4학년군은 47개 중 41개를 사용한다. 미사용 6개는 `[4수01-03]` 세 자리 수의 덧셈과 뺄셈, `[4수03-01]` 도형의 기초, `[4수03-13]`·`[4수03-14]` 시각과 시간, `[4수03-15]` 길이, `[4수01-12]` 소수다. 앞의 다섯은 전부 3학년 1학기에 몰려 있다.
- `grade3-semester1`만 4단원 16문항으로 규격 미달이다(나머지 7개 학기는 6단원 56~70문항). `grade3-semester1-u8b-draft.ts`에 20문항 초안이 남아 있다. 파일럿 범위 밖이라 2.1.0 발행 뒤 순서로 둔다.
- 2.1.0 발행 검토 주체를 변경했다: 다른 7개 학기와 동일하게 파일럿 운영자 본인이 콘텐츠 검수 체크리스트와 독립 검증 에이전트 승인만으로 검토·발행한다. 별도 두 번째 교사를 구하지 않는다.
- 2.2.0 내용 검수를 2026-08-07에 마쳤다: 64/64 통과, 수정 필요 0건. 따라서 파일럿 전 남은 순서는 `staging 6단원 배정 리허설 → production Supabase 신설·승격` 둘뿐이다. `VERCEL_TOKEN` 재발급, SSO 보호 해제, staging-retention 삭제 작업 검증, 2.1.0 발행 경로 복구는 2026-08-06에 완료됐다.
- 2.1.0 내용 검수를 사람·에이전트 세 갈래로 돌렸고 결과가 크게 엇갈렸다(2026-08-07). 한 에이전트는 17건 수정 필요, 다른 에이전트는 1건, 운영자 본인도 1건이었다. 17건을 문항 데이터에 하나씩 대조한 결과 11건이 실제 결함, 6건은 과장이었다. **양쪽 다 통과시킨 문항에서도 결함이 나왔다**(`g3s2-div-01`의 오답 `2개`는 오개념 ID와 어긋난다) — 에이전트 검수 결과는 근거를 재계산해 확인한 뒤에만 반영해야 한다.
- 확정한 결함은 네 갈래였다. (1) 지문이 정답을 미리 말하는 문항 2건(`g3s2-div-04`의 `6+6+6=18이에요.`, `g3s2-div-06`의 `쿠키를 7개씩`) — 나눗셈을 몰라도 맞힐 수 있어 진단 타당도가 무너진다. (2) 도출이 산술적으로 거짓이거나 재현 불가능한 오답 4건. (3) 짝문항과 오답 기제가 달라 같은 오개념을 두 번 관찰할 수 없는 오답 3건. (4) 이산량 문제에 연속량 표상을 붙인 시각자료 3건. 자동 검사는 이 넷 중 어느 것도 잡지 못한다.
- 오개념 ID에 `or`가 들어가면 신호다. `measurement.ton.ton-value-enlarged-or-dropped`는 "10배 확대"와 "t 성분 누락"이라는 다른 두 기제를 한 ID로 묶고 있었다. 한 ID를 둘로 쪼개려 했지만 `grade3-semester2-rationale.test.ts`가 "단계마다 오개념 2개를 두 문항에서 반복 관찰"을 강제하고 있어 막혔고, 대신 두 문항의 기제를 하나로 맞추는 쪽이 옳았다. 이 테스트가 설계 의도를 실제로 지키고 있었다.
- 발행된 2.1.0은 UPDATE·DELETE 트리거로 고칠 수 없으므로 수정은 2.2.0 신규 발행으로만 반영된다(`202608070001`). 2.1.0 마이그레이션은 grade4-semester1 1.3.0과 같은 방식으로 SHA-256 핀을 걸어 바이트 그대로 보존한다. 2.2.0 DB checksum은 `f744d7e1…4288e`.
- **발행은 문항 ID뿐 아니라 선택지 ID까지 영구히 고정한다.** `protect_published_diagnosis`는 새 발행본을 같은 set_key의 *모든* 기존 행과 대조하는데, 콘텐츠 패키지의 기존 검사는 각 세트의 최초 발행본 하나(`grade3Semester2Diagnosis`, 1.0.0)하고만 비교했다. 그래서 2.1.0에만 있던 선택지 ID 다섯 개를 지운 2.2.0이 로컬 테스트 620개를 전부 통과하고 CI의 `supabase start`에서야 `published stable IDs cannot be removed`로 막혔다(런 31158325892). 커밋된 발행 마이그레이션을 직접 읽어 트리거와 같은 것을 검사하는 `tools/published-stable-ids.test.ts`를 추가해 메웠다.
- 그 결과 오답 세 개는 라벨만 바꾸고 ID는 2.1.0의 옛 값(`25cm`, `about-80-each`, `7x5-plus6`)을 유지한다. ID가 옛 값을 이름으로 갖는 대가를 치르고 진단 품질을 택했다. 응답 원본을 눈으로 볼 때는 반드시 해당 버전의 콘텐츠와 join해야 라벨이 맞는다. `g3s2-graph-06`은 원자료의 마릿수를 2.1.0과 같게 두어 ID를 그대로 살렸고, `g3s2-measure-12`는 되돌렸다 — `or`가 들어간 것은 슬러그였고 교사에게 보이는 제목 "t 부분을 1000kg 크기로 반영하지 못함"은 확대와 누락을 이미 함께 덮는다. 슬러그만 `ton-value-misconverted`로 고쳤다.
- 검수 대상이 아직 아무 DB에도 없을 때만 같은 버전을 다시 생성할 수 있다. 2.2.0은 CI가 막힌 덕분에 재생성이 가능했다. main 푸시가 CI를 거쳐 staging 발행까지 자동으로 이어지므로, 그 경계를 넘은 뒤의 수정은 항상 다음 버전이다.

## Non-Goals

- 첫 파일럿에서 3–6학년 전체를 동시에 운영하지 않는다.
- 파일럿 전에 문제 수를 무조건 늘리지 않는다.
- AI가 학생 수준을 자동으로 확정하거나 처방하도록 만들지 않는다.
- 학생 간 순위나 비교 점수를 만들지 않는다.
- 첫 파일럿 결과를 일반화된 진단 타당성의 증명으로 사용하지 않는다.

## Risk Ledger

- 진단 분량이 길면 수학 수준보다 집중력과 기기 피로를 측정할 수 있다.
- 교사 분석지가 자세해도 다음 수업 행동으로 이어지지 않으면 핵심 가치가 검증되지 않는다.
- 규칙 기반 분석이 교사의 실제 관찰과 다를 수 있다.
- 한 학급 결과만으로 문항이나 진단 규칙을 성급하게 일반화할 수 있다.
- 기기·네트워크 문제를 학생의 응답 행동으로 오해할 수 있다.
- staging Vercel 배포가 무효화된 토큰으로 막혀 있어 교실 파일럿을 바로 시작할 수 없다(CI 자체는 통과, 배포만 막힘).
- 기준 발행본이 로컬 seed 에만 있어 신규 Supabase 프로젝트가 콘텐츠 없이 올라올 수 있었다. 발행본을 마이그레이션 체인으로 옮겨 해소했고, production 신설 시 `diagnosis_sets`에 1.0.0·2.1.0 두 행이 실제로 들어왔는지 확인한다.
- 보존 자료 자동 삭제가 작동하지 않은 상태에서 실제 학생 데이터를 수집하면 개인정보 보존 원칙을 지킬 수 없다.
- 본인 검토만으로 2.1.0을 발행하면 자동 검사를 사람의 최종 승인으로 오해하거나 콘텐츠 결함을 혼자 놓칠 수 있다. 다른 7개 학기에 이미 적용한 것과 같은 콘텐츠 검수 체크리스트·독립 검증 에이전트 승인 절차를 그대로 요구해 완화한다.

## Implementation Prompt

Middle of Math의 3학년 2학기 실교실 파일럿을 준비한다. 기존 Supabase 기반 구조와 repository 경계를 유지하고 Neon으로 마이그레이션하지 않는다. 교사는 발행된 학기에서 단원 하나를 선택해 배정하고 학생 문항, 서버 완료 조건, 교사 분석지는 같은 단원 범위만 사용한다. staging 배포용 무효화된 VERCEL_TOKEN을 재발급하고 retention 비밀값을 등록하며, 파일럿 운영자 본인이 콘텐츠 검수 체크리스트와 독립 검증 에이전트 승인으로 6개 단원·64문항인 grade3-semester2@2.1.0을 검토한 뒤에만 발행한다. staging에서 교사 로그인부터 학생 완료·반 요약·교사용 분석지까지 실제 설정으로 검증한다. 학생 25명이 태블릿으로 주 1단원씩 6주간 참여하며 단원당 15분을 사용한다. 교사는 별도 Google Form/Sheet에 매주 5명의 사전 관찰, 분석지 일치 여부, 후속 행동과 다음 주 효과를 기록한다. 학생 실명과 학교 식별 정보는 수집하지 않는다. 정의된 중단 기준을 코드·운영 체크리스트·리허설 시나리오에 반영하고, staging을 통과한 정확한 커밋만 production에 승격한다. AI 요약, 자동 처방, 보호자 계정, 다른 학년 확대는 이번 범위에서 제외한다.
