# Middle of Math — Phase 1 구현 결정

작성일: 2026-07-22
상태: 사용자 승인 후 구현 기준

## 범위 변경

- 첫 운영 진단은 `5학년 분수` 대신 `3학년 2학기 수학`으로 한다.
- 운영 화면에서는 곱셈, 나눗셈, 원, 분수, 들이와 무게, 그림그래프의 6개 단원을 다룬다.
- 기존 5학년 분수와 수 세기 데이터는 새 콘텐츠 패키지로 옮길 때 참고하는 legacy fixture로만 보존한다.
- 개인 리포트는 `교사용 리포트`와 `학부모 전달용 리포트`로 분리한다.
- Phase 1의 학부모 리포트는 교사가 확인한 뒤 인쇄하거나 PDF로 저장하는 전달용 화면까지 포함한다. 보호자 계정·이메일 자동 발송은 포함하지 않는다.

## 3학년 2학기 콘텐츠 기준

2022 개정 교육과정은 3~4학년군 성취기준을 제시하므로, `3학년 2학기` 순서는 Middle of Math의 진단 세트 manifest에서 관리한다. 교육과정 원문을 학생 문항에 복사하지 않고 안정적인 성취기준 ID만 앵커로 둔다.

| 단원 | 주요 앵커 | 진단 초점 |
|---|---|---|
| 곱셈 | `[4수01-04]` | 자릿값에 따라 부분곱을 구성하는가 |
| 나눗셈 | `[4수01-05]`, `[4수01-06]` | 등분·포함 의미, 몫과 나머지 |
| 원 | `[4수03-06]`, `[4수03-07]` | 중심·반지름·지름의 관계 |
| 분수 | `[4수01-09]`~`[4수01-11]` | 등분할, 분모·분자, 같은 분모·단위분수 비교 |
| 들이와 무게 | `[4수03-17]`, `[4수03-19]`, `[4수03-20]` | 단위 관계, 측정값 해석, 합과 차 |
| 그림그래프 | `[4수04-01]` | 범례를 수량으로 바꾸고 자료를 비교하는가 |

단원 순서는 2022 개정 교육과정을 반영한 3-2 참고서 목차와 3학년 2학기 수업 자료에서 교차 확인했다.

- [동아출판 2022 개정 3-2 목차를 인용한 도서 정보](https://gimssine.com/products/9788900481761)
- [경기도교육청 2025년 3학년 2학기 수업·평가 예시](https://www.goe.go.kr/resource/goe/na/bbs_2675/2025/07/f4c15fac-ae7b-46aa-8e16-b8d86b710d44.pdf)
- [2022 개정 수학과 교육과정 시안 성취기준](https://cdn.kosac.re.kr/files/legacy_data/jnrepo/upload/jnBrdBoard/202308/b800e7adf20d4ac2bf4892dddfb0fbcf_1692756609038.pdf)

## 학생 식별과 입장 정책

### 비교한 패턴

- Google Classroom은 6~8자리 영숫자 클래스 코드를 사용하고, 교사가 코드를 재설정하거나 비활성화할 수 있다. 계정이 있어 기기 간 등록 상태를 유지한다.
- Kahoot은 계정 없이 PIN과 닉네임으로 참여할 수 있지만, 나갔다 다시 들어오면 새 닉네임으로 재참여한다.
- Seesaw는 어린 학생에게 QR 또는 텍스트 코드를 제공하고, 클래스 코드를 학교·교사가 보호해야 하는 접근 수단으로 취급한다.
- Supabase anonymous user는 일반 `anon key`와 다르며 authenticated 역할로 RLS를 적용할 수 있지만, 브라우저 데이터를 지우거나 다른 기기를 쓰면 같은 anonymous identity를 복구할 수 없다.

참고:

- [Google Classroom 클래스 코드 입장](https://support.google.com/edu/classroom/answer/15605102?hl=en)
- [Kahoot PIN·닉네임 입장](https://support.kahoot.com/hc/en-us/articles/360039890713-Kahoot-join-How-to-join-a-Kahoot-game)
- [Seesaw 학생 로그인 방식](https://seesaw.com/get-started/)
- [Supabase anonymous sign-in과 RLS](https://supabase.com/docs/guides/auth/auth-anonymous)

### Middle of Math 결정

- `학생 번호`를 클래스 안의 필수 안정 식별자 `roster_key`로 사용한다.
- `별칭`은 선택 표시명이다. 교사·학부모 화면에서 번호와 함께 또는 번호 대신 보여줄 수 있다.
- 실명 입력 필드는 만들지 않는다.
- 클래스 코드는 혼동하기 쉬운 문자를 뺀 6자리 대문자·숫자로 자동 생성한다.
- 교사는 코드를 재발급하거나 즉시 비활성화할 수 있다.
- 같은 브라우저에서는 anonymous auth와 IndexedDB를 이용해 자동으로 이어간다.
- 새 기기에서는 기존 세션을 덮어쓰지 않고 새 시도로 저장한다. 교사는 충돌 상태를 확인하고 사용할 세션을 결정한다.
- 완료되지 않은 세션은 반 오개념 집계에서 제외하고 `진행 중` 인원으로만 표시한다.

## 교사용과 학부모용 리포트 분리

Google Classroom은 교사 화면의 상세 학생 작업과 보호자 요약을 분리하고, 보호자는 교사·학생 화면 전체에 접근하지 않는다. Middle of Math도 같은 경계를 사용한다.

- 교사용: 오개념 후보, 교육과정 앵커, 판단 단위 이벤트, 시간·변경·불확실성 신호, 엔진 버전
- 학부모용: 관찰한 강점, 현재 연습할 한두 가지, 가정에서 사용할 짧은 질문, 확정 진단이 아니라는 안내
- 학부모용에서 제외: 다른 학생과의 비교, 반 순위, 원본 이벤트, 오개념 코드, 엔진·콘텐츠 내부 ID

참고: [Google Classroom 보호자 이메일 요약의 접근 범위](https://support.google.com/edu/classroom/answer/7126518?hl=en)

## 해석 규칙 v3

학생의 한 번의 선택만으로 부족한 학습 단계를 확정하지 않는다. `rules-3.0.0`은 학생 이벤트를 바꾸지 않고 같은 입력을 다시 재생해 `추가 관찰`과 `반복 확인`을 구분한다.

| 조건 | 교사 화면 | 보호자 공유본 |
|---|---|---|
| 같은 신호가 한 문항에서만 관찰됨 | `한 번 관찰` | 연습 항목에 넣지 않음 |
| 같은 신호를 확인할 문항이 한 개뿐임 | `한 번 관찰` | 연습 항목에 넣지 않음 |
| 서로 다른 두 문항에서 같은 신호가 관찰됨 | `반복 확인` | 최대 2개까지 연습 항목에 포함 |
| `잘 모르겠어요` 또는 정의되지 않은 선택 | `한 번 관찰` | 연습 항목에 넣지 않음 |
| 응답 위치 편향 또는 모든 근거가 지나치게 빠름 | `한 번 관찰` | 연습 항목에 넣지 않음 |
| 이전 `rules-2.0.0` 저장 결과 | `한 번 관찰` | 새 엔진 재해석 전 확정하지 않음 |

반복 확인에는 서로 다른 판단 ID가 2개 이상 필요하고, 해당 신호를 관찰할 기회도 2개 이상이어야 한다. 정답 반응은 반대 근거로 함께 보여 주되 이미 두 문항에서 반복된 관찰을 지우지는 않는다. 위치 편향은 적어도 5개 응답, 제시 순서 근거 80% 이상, 한 위치 선택 80% 이상일 때만 근거 품질 경고로 사용한다. 선택까지 1.5초 미만이면서 전체 응답도 3초 미만인 기록만 지나치게 빠른 응답으로 본다.

이 임계값은 교사가 조절하는 평가 기준이 아니라 얇은 근거를 확정하지 않기 위한 보수적 엔지니어링 기준이다. 임계값을 바꾸면 규칙 엔진 버전도 함께 올리고, 이전 해석 실행은 수정하거나 삭제하지 않는다.

## UI 방향

- DESIGN.md의 색·타이포·간격 토큰을 그대로 코드 토큰으로 만든다.
- 학생 화면의 기억 장치는 `판단선`: 텍스트 단계 번호 없이 한 칸씩 전진하는 가는 진행선이다.
- 교사 화면의 기억 장치는 `근거선`: 교육과정 앵커에서 작은 학습 단계와 실제 선택까지 이어지는 세로 연결선이다.
- 학생 화면은 차분하고 넓게, 교사 화면은 표보다 우선순위와 근거 연결이 먼저 보이게 한다.
- 학부모 리포트는 A4 인쇄 시 한 장에 들어가는 단일 열 문서로 만든다.
# 교육과정 교차표는 런타임 진단과 분리한다

2026-07-29부터 3학년 2학기 콘텐츠의 17개 성취기준과 32개 진단 단계를 고정된 `DECK6/korean-elementary-learning-map` 커밋에 대조한다. 외부 자료는 성취기준·주제·선수 관계를 찾는 편집 근거이며, 학생 응답을 해석하는 진단 규칙에는 들어가지 않는다.

외부의 성취기준 수준 주제와 Middle of Math의 더 작은 진단 단계가 같다고 주장하지 않기 위해 현재 32개 단계는 모두 `topic-partial`이다. 외부 선수 관계 6개도 후보로만 남기며 로컬 선수 단계로 자동 승격하지 않는다. 고정 snapshot과 교차표의 SHA-256이 달라지거나 행·범위·라벨이 어긋나면 2.x 발행을 차단한다.

# U6 오답 근거와 심각도 구현 결정

작성일: 2026-07-30
상태: `2.1.0` 자동 검증 통과, 교사 내용 검수 대기

## D1. 런타임 신호는 단계마다 하나를 유지한다

오답 128개를 설명하기 위해 런타임 신호를 64개로 늘리지 않는다. 학생 응답
해석은 기존 단계 신호를 유지하고, 더 세밀한 오개념 ID·제목·근거·도출 과정은
편집 블루프린트에만 둔다. 이렇게 해야 기존 이벤트와 리포트 호환성을 지키면서
오답끼리 다른 사고 과정을 검수할 수 있다.

## D2. 오답 근거는 선택지에서 역산한다

먼저 실제 오답 값이 나오는 계산 또는 잘못 적용한 판단 규칙을 한 줄로 재현한다.
그 뒤 교사가 확인할 수학적 판단을 별도 문장으로 적는다. 선택지 자체를
되풀이하거나 신호 문구와 다른 오답 설명을 복사하면 발행 게이트가 차단한다.
각 오개념 ID는 학습 단계 ID로 시작하며 32개 단계마다 2개씩, 모두 64개다.

## D3. 심각도는 검토 우선순위이며 자동 진단이 아니다

핵심 개념 관계는 `high`, 주요 계산·표현 절차는 `medium`, 보조 전략은 `low`로
검토했다. 기존 14개 신호는 바꾸지 않고 추가 20개에만 기준을 적용했다.
전체 34개 분포는 `high 10 / medium 18 / low 6`이고, fallback 2개를 뺀
진단 신호 32개는 `high 10 / medium 18 / low 4`다. 한 번 관찰된 신호는
심각도와 무관하게 확정하지 않으며 `rules-3.0.0`의 반복 근거 기준을 그대로 쓴다.

## D4. 편집 근거는 학생과 학부모에게 보내지 않는다

오개념 제목, 오답 근거, 도출 과정, 공통 신호 설명은 `DiagnosisSet`과 분리한다.
학생 앱은 답 후보만 받고, 학부모 공유본은 반복 확인된 지원 영역만 받는다.
스튜디오는 이 원장을 읽기 전용으로 표시한다. 초안의 선택지 ID가 원장과
어긋나면 근거를 추측해 연결하지 않고 명시적인 불일치 경고를 보여 준다.

진단 무결성 게이트 `gate-1.0.0`, 블루프린트 `2026-07-30.2`, 콘텐츠
`2.1.0`의 checksum을 함께 고정한다. 자동 오류는 0개이며, 외부 학습맵의
후보·자문 상태를 알리는 출처 경고 2개는 의도적으로 유지한다.

# U8 초3-1 선수학습 연결 구현 결정

작성일: 2026-07-30
상태: 자동 검증 통과·교사 내용 검수 대기

## D1. 초3-1은 초3-2 안에 섞지 않고 별도 진단 세트로 둔다

`grade3-semester1` `1.0.0`을 `review` 상태로 만든다. 4개 단원, 8개 단계,
16개 판단을 두며 각 단계는 직접 확인과 적용·전이 판단을 하나씩 가진다.
초3-2 `2.1.0`의 문항, checksum, 블루프린트, 교차표는 바꾸지 않는다.

## D2. 고정 원본과 다른 성취기준 추정을 거부한다

초기 계획의 `[4수03-05]` 평면도형 제안은 고정된 DECK6 원본과 맞지 않아
철회했다. 길이는 `[4수03-16]`으로 연결한다. 외부 자료는 3~4학년군까지만
확인하므로 모든 1학기 배치는 로컬 편집 결정이라고 교차표에 명시한다.

## D3. 학기 사이는 별도 자문 그래프로 연결한다

세트 내부 `prerequisiteStageIds`는 같은 학기의 순서를 유지한다. 초3-1에서
초3-2로 향하는 6개 연결은 digest가 있는 별도 그래프에 두고 모두
`advisory: true`로 고정한다. 교사 화면에는 편집 참고로만 보여 주며 진단
엔진, 학생 화면, 학부모 공유본에는 자동 판정으로 전달하지 않는다.

## D4. 나머지 1학기 범위는 근거 검수 뒤 U8b에서 확장한다

평면도형, 시간, 덧셈과 뺄셈, 소수는 문항 수를 채우기 위해 추정해 넣지
않는다. 공식 학기 배치와 한 문항당 관찰 행동을 확인한 뒤 별도 버전으로
추가한다. 현재 세트는 자동 게이트 통과가 발행 승인을 뜻하지 않으며 다른
교사의 내용 검수를 받아야 한다.

## D5. 학년군 성취기준과 로컬 학기 배치를 DB에서 분리한다

교육과정이 3~4학년군으로 제시한 `[4수01-04]`, `[4수01-05]`,
`[4수01-06]`, `[4수01-09]`는 `shared_across_semesters=true`로 표시한다.
발행 가드는 같은 학년이면서 공통 앵커이거나 정확히 같은 학기인 앵커만
허용한다. 이 구조로 초3-1이 공통 수와 연산 기준을 정식으로 참조하면서도
`[4수03-16]`과 `[4수03-17]` 같은 학기 전용 편집 배치가 섞이지 않게 한다.
콘텐츠 스튜디오의 학기 필터도 해당 학기 전용 앵커와 공통 앵커를 함께
조회한다.

## D6. 학생에게 필요한 수학 관계는 의미 시각자료로 표현한다

분수 등분할은 `partition-diagrams`로 조각의 실제 너비 비율을 그리고,
길이 단위 변환은 `length-relation`으로 시작 단위와 목표 단위를 보여 준다.
TypeScript, UI, 스튜디오, DB 발행 스키마가 같은 visual kind를 검증한다.
초3-1 콘텐츠 checksum은
`36075f86f51e8b7c19f5df87b681a1b98fc9db84d2d0415d6fcd48d11aa1d56d`로
고정하며 런타임에서 자기 자신을 다시 계산해 덮어쓰지 않는다.

선수 그래프의 검수 설명·외부 ID는 편집 패키지에만 남긴다. 교사 앱은
자문 연결 표시를 위해 `id`, `from`, `to`, `advisory`만 담은 최소 투영을
사용하며 학생·교사 번들 누출 검사에서 전체 검수 문구를 금지한다.

# U9 초4 확장 기반 구현 결정

작성일: 2026-07-30
상태: U9.0-a 인프라 구현·독립 검증 대기

## D1. 학년군 출처와 특정 학년 사용 승인을 분리한다

`grade_band="3-4"`는 교육과정이 성취기준을 제시한 범위를 기록할 뿐,
그 성취기준을 초3 또는 초4 어느 학기에 배치해도 된다는 승인이 아니다.
DB는 다음 세 축을 따로 관리한다.

- `grade_band`: 원문의 학년군 출처
- `shared_across_semesters`: 같은 학년의 다른 학기 사용 승인
- `shared_across_grade_band`: 같은 학년군의 다른 학년 사용 승인

발행 정합성 가드는 학년과 학기를 각각 검사한다. 원래 학년이 아니면
`shared_across_grade_band=true`이고 manifest 학년이 `grade_band` 안에
있어야 한다. 원래 학기가 아니면 별도로
`shared_across_semesters=true`여야 한다. 학년군 값만 보고 자동 승인하지
않는다.

현재 등록된 `[4수]` 앵커 18개에는 `grade_band="3-4"`만 기록하고
`shared_across_grade_band`는 모두 false로 둔다. 초4 코드→단원→학기 표를
교과 검수자가 승인하기 전에는 기존 초3 앵커를 초4에서 사용할 수 없다.
pgTAP은 트랜잭션 안에서만 한 앵커의 학년군 공유 플래그를 켜 양성 경로를
검증하고 마지막에 되돌린다.

TypeScript 레지스트리도 같은 원칙으로 `gradeBand`,
`sharedAcrossSemesters`, `sharedAcrossGradeBand`를 구분한다.
`findAnchorForSet()`은 정확한 학년·학기를 우선하고, 학년군 공유가
명시되지 않았거나 서로 다른 라벨 후보가 겹치면 추측하지 않고
`undefined`를 반환한다. 현재 조회에는 검수된 최신 라벨을 사용하고,
이미 고정된 3-2 v1 checksum 재현에만 `v1Label`을 사용한다.

## D2. 선수 그래프는 소유자와 중복 범위를 명시한다

학년별 그래프가 늘어날 때 다음 오류를 발행 전에 차단한다.

- `SPG_EDGE_OUTSIDE_OWNER`: 출발과 도착 어느 쪽에도 그래프 소유 세트가 없음
- `SPG_EDGE_DUPLICATED_ACROSS_GRAPHS`: 같은 출발·도착 연결을 여러 소유
  그래프에 중복 등록
- `SPG_GRADE_SKIP_UNJUSTIFIED`: 중간 학기를 하나라도 건너뛰어 연결하면서 구체적인
  건너뛰기 근거가 없음

중간 학기를 하나라도 건너뛰는 연결은 검수 근거에 `학기 건너뛰기:` 표식과 10자 이상의
이유를 남긴다. 이 규칙은 장거리 연결을 금지하기보다, 중간 학기의
관련 단계가 왜 연결에 포함되지 않았는지 사람이 다시 확인할 수 있게 한다.

U9.0-a에서는 초4 문항·앵커·스냅숏을 추가하지 않는다. 기존 초3-1,
초3-2 세트와 고정 그래프·교차표·스냅숏은 그대로 유지한다.

## D3. 초4 학기 배치는 별도 검토 원장으로 승인받는다

U9.0-b에서 고정 DECK6 커밋의 3~4학년군 전체 47개 성취기준을
`kr-learning-map.grade34.snapshot.json`으로 보존했다. 스냅숏은
47개 기준, 142개 주제, 6개 선수 후보만 담으며 학생 진단을 지원하지
않는다고 명시한다. 코드마다 업스트림의 `module`, `focus`, 공식 자료
locator를 함께 보존한다. 원본 3파일의 해시와 스냅숏 digest를 테스트로
고정한다. locator의 기준 문서인 NCIC `[별책8] 수학과 교육과정.pdf`도
URL·바이트 수·쪽 수·SHA-256을 `sources`에 보존해 같은 원문에서 위치를
다시 확인할 수 있게 한다. 다만 DECK6가 기록한 `pdftotext` 줄 번호는
추출 도구가 고정되지 않아 로컬에서 재현되지 않으므로
`locatorProvenance="upstream-recorded"`,
`locatorLocallyReproducible=false`로 명시하고 승인 근거에서 제외한다.
대신 사람이 검토할 수 있는 상류 `evidence.basis` 문장을 함께 보존한다.

4학년 코드→단원→학기 배치는 경기도교육청의 2025학년도 3~4학년
1·2학기 수업-평가 계획 예시와 대조해
`grade4-curriculum-placement.json`에 기록한다. 전역 상태는
`pending-teacher-review`이며 4-1·4-2 각각 6개 단원이다. 2026-07-30
A1에서 `teacher:workspace-owner`가 4-1 큰 수와 각도 두 단원만
부분 승인했고, 나머지 10개 단원은 승인 대기 상태를 유지한다.
`[4수04-03]`은 두 학기에 모두 나타나지만, 승인 전에는 학기 공유로
등록하지 않는다. 기존 초3에도 쓰인 네 코드의 학년군 공유 여부도 교사가
코드별로 승인한다. 단원별 전체 코드 목록과 두 공유 후보 집합은 테스트로
고정하고, 공식 게시물 첨부 PDF의 주소·바이트 수·SHA-256은 검토 원장에
기록하되 파일 자체는 복제하지 않는다. 2학기 PDF 부록에는 학년군 전체
47개 코드가 있으므로 코드의 단순 존재를 학기 배치 근거로 쓰지 않고,
단원 문맥과 교사 승인을 별도로 요구한다.

전체 원장의 전역 상태와 별도로 각 단원에 `reviewStatus`, `reviewedBy`,
`reviewedAt`을 둔다. `inspectGrade4PlacementApproval()`은 초4 진단
세트가 승인된 단원만 선언하고 그 단원의 성취기준을 빠짐없이 포함하는지
검사한다. 이 검사는 아직 coverage blueprint가 없는 초안에도 적용해,
큰 수·각도 A1 승인만으로 뒤 단원이나 공유 후보가 함께 열리지 않게 한다.
검사는 setKey와 manifest의 ID·학년·학기도 대조한다. 승인자 ID는
`teacher:<식별자>`, 승인 시각은 시간대가 있는 ISO-8601이면서 현재
시각보다 늦지 않아야 한다. 전역 `approved`와 12개 단원의 전체 승인은
항상 함께 바뀌어야 한다. Studio에는 동결된 읽기 전용 승인 요약만
노출하고 학생 런타임 export에는 포함하지 않는다.

# A2-5 초4-1 곱셈과 나눗셈 구현 결정

작성일: 2026-07-31
상태: 구현·Codex 전체 회귀·Kiro Opus 5 독립검증 통과

## D1. 공유 성취기준은 세트별 허용 목록으로만 연다

`[4수01-04]`, `[4수01-05]`, `[4수01-08]`의 기존 초3 canonical 행은
바꾸지 않는다. 초3-1·초3-2·초4-1 가운데 교사가 검수한 조합만
`curriculum_anchor_set_allowlist`에 기록한다. `[4수01-07]`은 초4-1
canonical 행으로 등록하며 초4-2는 허용하지 않는다. 이 결정으로
`shared_across_grade_band`를 켜서 학년군 전체를 자동 승인하는 경로를
사용하지 않는다.

## D2. 계산 단원도 작은 판단 단계로 분리한다

단원 전체 정답률 대신 몇십의 자리값, 부분 계산 합치기, 몫의 첫 자리,
몫 조정, 나눗셈 검산식, 결과 어림을 여섯 단계로 나눈다. 각 단계는
direct·transfer 두 판단과 같은 두 오개념을 가져, 한 번의 오답만으로
부족을 확정하지 않고 반복 근거를 수집한다.

## D3. 불필요한 그림을 만들지 않는다

12문항은 수식과 짧은 문장만으로 수학적 판단이 완결된다. 장식용
세로셈 그림이나 정답을 암시하는 도식을 추가하지 않고 `visual:none`을
사용한다. 대신 세 화면 크기에서 어절 내부 줄바꿈과 가로 넘침을
실브라우저로 검사한다.

## D4. 1.3.0은 불변으로 두고 1.4.0을 추가한다

`grade4-semester1@1.3.0`의 54문항·108개 오답 해석은 수정하지 않는다.
`1.4.0`은 여기에 12문항·24개 해석을 append-only로 더한
6단원·33단계·66문항 발행본이다. 신규 설치와 이전 seed 업그레이드를
각각 격리 DB에서 재생하고 두 버전의 checksum·해설 수를 함께 고정한다.
DB 테스트는 `1.4.0`의 체크섬 상수와 manifest 내부 체크섬이 모두
일치해야 통과하도록 해 생성기나 seed의 우발적 변화를 막는다.

# A3-2 초4-2 분수의 덧셈과 뺄셈 구현 결정

작성일: 2026-07-31
상태: 구현·Codex 전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 분수막대가 계산 답을 먼저 보여 주지 않게 한다

동분모 덧셈·뺄셈 문항에서 채운 칸과 전체 칸을 함께 보여 주면,
학생은 분모를 유지하는 원리를 적용하지 않고도 정답의 분자와 분모를
셀 수 있다. 따라서 A3-2의 10문항은 `visual:none`을 사용하고,
피연산자·연산·답의 형태·단위를 수식과 짧은 문장에 모두 보인다.
분수막대를 다시 넣는 변경은 전용 회귀 테스트가 차단한다.

## D2. 한 단계에 한 계산 행동만 둔다

동분모 진분수 덧셈, 합의 대분수 변환, 동분모 진분수 뺄셈,
1에서 진분수 빼기, 받아올림 없는 대분수 덧셈을 각각 분리한다.
분모가 다른 분수, 약분, 받아올림과 받아내림은 이번 진단 범위에서
제외한다. 대분수 변환이 필요한 단계의 교사 해석에는 이전 학년의
가분수·대분수 관계를 먼저 확인할 수 있음을 명시한다.
여기서 받아올림·받아내림 제외는 이미 자연수 부분이 있는 대분수끼리
계산할 때 분수 부분에서 1을 넘기거나 자연수 부분에서 1을 빌리는 경우를
뜻한다. 진분수의 합을 처음 대분수로 바꾸는 단계와 `1`을 같은 분모의
가분수로 바꾸는 단계는 단원 안의 별도 기초 행동으로 포함한다.

## D3. 정답 표시를 신뢰하지 않는 수학 오라클을 둔다

오라클은 문항별 두 피연산자와 연산에서 정답을 다시 계산하고,
두 오답도 선언된 잘못된 계산 규칙으로 재현한다. 선택지 문자열이
다르더라도 같은 유리수라면 실패하며, 동분모 유지·받아올림 없음·
대분수 과·와 표기·단위 일치를 함께 검사한다. 대분수 답은 질문에서
형태를 명시하고, 오답이 문맥에 이미 보이는 피연산자와 우연히 같아
두 오개념으로 해석되지 않게 한다.

## D4. 새 문항이 드러낸 DB 시각 계약의 빈틈도 함께 닫는다

분수 단원은 `visual:none`만 사용하지만, 발행 경계에는 기존
`fraction-bar` 계약도 남아 있다. 신규 DB 음성 테스트가 TypeScript에서는
거부되는 알 수 없는 `answer` 키를 최신 DB 검증 함수가 받아들이는 회귀를
발견했다. 기존 migration을 수정하지 않고 후속 검증 래퍼를 추가해
`kind`, `numerator`, `denominator`, `unknown` 이외 키를 차단했다.
신규 설치 전체 migration 재생과 18개 DB 테스트 파일을 함께 통과해야
A3-2 검증 완료로 본다.
새 외곽 `security definer` 함수에는 기존과 같은
`REVOKE ... FROM PUBLIC`을 다시 적용하고, `anon`과 `authenticated`
역할의 직접 실행 불가를 pgTAP으로 고정한다.

## D5. 성질 문항은 다른 계산으로 우회해 풀 수 없게 한다

첫 독립 검수에서 A3-1의 이등변삼각형 두 각 문항이 세 각의 합만으로도
풀린다는 약점을 발견했다. 같은 변 눈금과 한쪽 맞은각만 보여 주고,
물어보는 다른 맞은각과 세 번째 각은 수치로 보여 주지 않도록 계약을
바꿨다. TypeScript·Zod·SQL·SVG·접근성 설명·수학 오라클은 같은 규칙을
검사한다. 세 번째 각을 다시 표시하여 정답을 각의 합으로 얻을 수 있게
하면 발행 전 테스트가 실패한다.

## 완료 판정

Kiro CLI Claude Opus 5의 두 번째 독립 검증은 `VERDICT: APPROVE`,
출시 차단 항목 0건을 반환했다. 검증자가 직접 46파일 368테스트,
DB 18파일 282테스트, Playwright 27시나리오, 타입 검사와 빌드를
다시 실행해 모두 통과했다. 첫 검증에서 발견한 DB 함수 권한과
성질 문항의 우회 풀이도 실제 권한 질의와 SVG 기하 계산으로 종결했다.

# A3-3 초4-2 사각형 구현 결정

작성일: 2026-07-31
상태: 구현·전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 도형의 모양이 아니라 좌표에서 수학적 사실을 계산한다

네 꼭짓점을 0~24 범위의 정수 격자점으로 저장하고, 벡터의 내적과
외적으로 수직·평행·볼록성을 계산한다. 변의 길이는 길이의 제곱,
평행선 사이 거리는 외적 넓이와 정사영으로 다시 계산한다. 콘텐츠에
적힌 표시가 계산 결과와 다르면 TypeScript·Zod·SQL 모두 거부한다.

## D2. 표시 하나가 관찰하려는 행동 하나를 가리키게 한다

수직 문항은 직각 표시, 평행 문항은 한/두 화살표, 마름모 문항은 같은
눈금을 사용한다. 색은 장식일 뿐 의미를 전달하지 않는다. 학생 문구도
`직각 표시가 있는 곳만`, `같은 눈금이 있는 변끼리만`처럼 그림에서
확인할 근거를 짧게 안내한다.

## D3. 사각형 포함 관계의 모호성을 이번 선택형 진단에서 피한다

사다리꼴 문항의 도형은 평행한 변이 정확히 한 쌍이며 선택지를
사다리꼴·평행사변형·마름모로 둔다. 마름모 문항은 네 변이 같고 직각이
없는 도형으로, 선택지를 마름모·정사각형·직사각형으로 둔다.
`정사각형도 마름모인가` 같은 포함 관계 문항은 별도 교수 설계 전에는
내지 않는다.

## D4. 선택형 문항이 관찰하지 못한 능력을 교사에게 확정하지 않는다

`[4수03-03]`은 그리기와 설명도 포함하지만 이번 활동은 표시를 읽어
고르는 진단이다. 다섯 교사용 신호에는 자유 그리기·도구 조작·풀이
설명은 관찰하지 않았다고 명시하고, 교사가 말하기나 그리기로 후속
확인할 방법을 제공한다.

## D5. 화면의 글자 위치와 저장 권한까지 같은 하네스가 막는다

라벨 기준점은 240×180 SVG 안에 있고 서로 14단위 이상 떨어져야 한다.
Playwright는 데스크톱·태블릿·390px 휴대전화에서 10문항을 완주하며
실제 글자 경계 상자가 겹치지 않는지 검사한다. DB 검증기는 알 수 없는
키, 틀린 표시와 수치, 교차 도형을 거부하며 `PUBLIC` 실행 권한을
회수해 `anon`과 `authenticated`가 내부 검증 함수를 직접 호출하지
못하게 한다.

## D6. 기준 변을 읽지 않아도 풀리는 시각 지름길을 허용하지 않는다

첫 독립 검증에서 수직 문항의 직각 표시 두 개를 잇는 변이 언제나
정답이라는 지름길을 발견했다. 기준 변의 평행한 맞은편 변과 수직인
옆변을 함께 만들면 직각 두 개가 생기는 구조적 문제였다. 수직 모드는
평행한 변을 허용하지 않는다. 후속 독립 검증에서 직각 표시 하나에
닿는 선택지만 골라도 정답이 되는 더 약한 지름길까지 확인하여,
서로 마주 보는 두 꼭짓점에 직각 표시를 두고 세 선택지가 모두 표시
하나에는 닿도록 보완했다.
두 오답은 기준 변과 만나지만 직각이 아닌 변, 기준 변과 만나지 않는
변으로 구성한다. 질문의 기준 변 이름과 `baseSideIndex`도 스키마에서
교차 확인하므로 학생은 기준 변과 직각 표시를 함께 읽어야 한다.

## D7. 문항별 보완을 재사용 가능한 제작 계약으로 올린다

질문에는 기준 변 하나만 적도록 하고 TypeScript와 DB가 같은 이름을
확인한다. 수직 모드 전체는 직각 표시 두 개의 대칭 위치, 모든 선택
대상 변의 표시 접촉, 미표시 각의 직각 이격을 공통으로 검사한다.
정수 내적 비교식도 두 계층에서 동일하게 사용한다. 콘텐츠 검수
스킬·루브릭·정적 감사에는 기준 변과 직각 표시 규칙을 추가해 새
문항을 만들 때 같은 지름길이 다시 들어오지 않게 한다.

최종 Kiro CLI Claude Opus 5 읽기 전용 검증은 `VERDICT: APPROVE`,
차단 항목 0건을 반환했다. 58개 경계·중복·프롬프트 차등 입력에서
TypeScript/Zod와 SQL 불일치가 없었고, 콘텐츠 387테스트·DB
310테스트·Playwright 28시나리오·타입 검사·빌드가 모두 통과했다.

# A3-4 초4-2 소수의 덧셈과 뺄셈 구현 결정

작성일: 2026-07-31
상태: 구현·검증 완료

## D1. 읽기·쓰기는 소수 세 자리까지, 연산은 소수 두 자리까지 둔다

`[4수01-13]`의 범위를 빠뜨리지 않기 위해 읽기·쓰기 문항에는 소수
세 자리 수를 포함한다. 비교·덧셈·뺄셈은 `[4수01-16]`에 맞춰 소수
두 자리 수 범위만 사용한다.

## D2. 그림으로 답을 먼저 보여 주지 않는다

자릿값 모눈과 소수막대는 칸 수로 정답을 읽게 할 수 있어 열 문항 모두
`visual:none`을 사용한다. 학생에게는 짧은 수식과 한 단계 생활 맥락만
보이고, 제작용 오개념 이름과 산출 과정은 런타임 번들 밖에 둔다.

## D3. 소수를 부동소수로 계산하지 않는다

읽기·쓰기 값은 1000분의 1 정수, 자릿값·비교·연산 값은 100분의 1
정수로 바꾸어 정답과 두 오답을 다시 계산한다. 덧셈은 오른쪽 끝 맞춤과
받아올림 누락, 뺄셈은 자리별 큰 수 빼기와 빌려 준 자리 감소 누락을
direct·transfer에서 동일하게 반복한다.

## D4. 자리값 문항을 소수의 곱셈처럼 보이게 만들지 않는다

첫 독립 검증에서 `0.01 kg짜리 물체 62개` 맥락은 의도와 달리 소수와
자연수의 곱셈을 요구하는 문항으로 읽혔다. 전이 문항도 직접 문항과
같이 `0.1 kg이 6개, 0.01 kg이 2개`라는 자리값 합성만 보이도록
고쳤다. 회귀 테스트는 소수 세 자리 연산, 곱셈·나눗셈, 단위 환산,
같은 소수 단위 묶음의 곱셈 맥락을 차단하고 열 문항의 값을 정수
스케일로 다시 계산한다.

## D5. 선택지 셔플과 질문 속 지름길을 따로 검사한다

마지막 독립 검증은 두 비교 문항 모두 문장에 처음 나온 값이 가장 큰
값이라는 약한 지름길을 발견했다. 직접 문항의 수를 `1.25, 1.4,
0.98` 순으로 바꾸어 정답의 원문 위치를 direct·transfer에서 다르게
했다. 콘텐츠 검수 스킬과 오라클은 이제 선택지 위치가 아니라 맥락·표·
그래프 안의 원자료 순서도 검사한다.

Kiro CLI Claude Opus 5의 읽기 전용 재검증은 이전 고위험 항목이
해소됐음을 확인하고 `VERDICT: APPROVE`, 차단·고위험 0건,
`NEXT_UNIT_READINESS: READY`를 반환했다. 승인 뒤 남은 비교 지름길도
추가로 제거했다. 최종 회귀는 콘텐츠 포함 48파일 392테스트, DB
20파일 318테스트, Playwright 29시나리오, 8개 workspace 타입 검사와
전체 빌드를 통과했다.

# A3-5 초4-2 다각형 구현 결정

작성일: 2026-08-01
상태: 구현·전체 회귀·Kiro Opus 5 최종 독립 검증 통과

## D1. 다각형의 정의를 선분 수로 우회하지 못하게 한다

다각형 찾기 후보 세 개는 보이는 선분 수를 같게 두고, 닫힌 곧은선
도형 하나·열린 곧은선 도형 하나·굽은 선이 든 닫힌 도형 하나로
구성한다. 열린 두 끝에는 작은 끝점 표시를, 굽은 변에는 휴대전화에서도
보이는 곡률을 준다. 정답은 후보 ID가 아니라 실제 폐곡선 조건에서
다시 계산한다. 열린 끝점은 오답을 암시하는 오류색 대신 중립색을 쓰며,
접근성 설명은 현재 후보 순서와 실제 선 자료에서 생성하되 정답 명칭을
말하지 않는다.

## D2. 정다각형의 두 조건을 각각 분리한다

정답 한 개와 함께 변만 모두 같은 오답, 각만 모두 같은 오답을 같은
변의 수로 제시한다. 좌표나 구성 데이터에서 변·각의 동치류를 계산해
눈금과 호를 그리므로 장식 표시와 실제 기하가 어긋나면 스키마가
거부한다. 학생 질문은 정의를 다시 써 주지 않고 `정다각형을 골라
보세요`로 줄였으며, 초등학생에게 낯선 `호` 표현도 제거했다. 390px
화면은 후보를 2열로 바꾸어 눈금과 각 표시의 최소 판독 크기를 확보한다.

## D3. 같은 넓이와 실제로 들어맞는 모양을 구분한다

모양 조각은 방향이 있는 단위 정삼각형 칸의 집합으로 저장한다.
회전·이동한 정삼각형, 마름모, 사다리꼴, 정육각형을 exact-cover로
탐색해 남은 자리를 채우는 후보가 정확히 하나인지 확인한다. 두 문항
모두 칸 수는 같지만 테두리가 맞지 않는 오답을 포함한다. 구현 중
“정육각형에서 마름모를 빼면 다른 마름모 두 개가 못 들어간다”는 초기
가정이 거짓임을 탐색기가 발견해, 실제로 맞지 않는 오목한 자리로
교체했다.

첫 독립 검증에서 모든 조각 SVG를 같은 상자에 늘여 그린 탓에 1칸
정삼각형과 6칸 정육각형이 같은 크기로 보이는 결함도 확인했다. 모든
조각과 보드는 공통 단위 정삼각형 축척으로 렌더하고, 학습자가 직접
세어야 하는 `빈자리 N칸` 표시는 제거했다.

## D4. 선택형 진단의 관찰 한계를 교사 신호에 남긴다

다섯 단계는 다각형 구별, 변 수로 이름 짓기, 정다각형 구별, 남은 자리
채우기, 한 조각의 크기로 개수 정하기다. 자유 작도·도구 조작·여러
방법 만들기와 설명은 관찰하지 않았다고 모든 관련 교사 해석과 후속
지도 문구에 명시한다.

## D5. 모든 계층이 같은 그림을 거부한다

TypeScript·Zod는 좌표 기하와 exact-cover를, SVG는 같은 좌표와 격자
셀을 사용한다. 첫 독립 검증에서 SQL이 교차 도형, 볼록한 변 세기 도형,
끊어진 격자, 합동이 아닌 조각과 exact-cover 실패를 통과시키는 8개
차등 반례가 발견되었다. 후속 마이그레이션은 선분 교차·오목성·변 길이
비율·정다각형·셀 연결성·조각 합동·정확 덮기를 SQL에서도 계산한다.
19개 pgTAP 반례로 이 차이를 고정했고 내부 디스패처의 PUBLIC 실행
권한도 회수했다.

오개념 ID도 더 이상 `choiceA/choiceB` 위치에서 만들지 않는다. 각
오답이 실제로 보이는 오류 기제에 명시적으로 연결되며, direct와
transfer에서 열린 모양과 굽은 모양의 위치가 바뀌는 사례를 회귀
테스트한다. 콘텐츠 검수 스킬에는 같은 규칙을 추가했다.

두 번째 독립 검증은 조각 수 직접 문항의 `2개` 오답이 오개념 제목과
다른 경로에서만 나오고, 전이 문항 도형이 직접 문항 도형을 그대로
포함한다는 새 결함을 발견했다. 두 격자를 독립 모양으로 바꾸고 두
오답 모두 `정답 조각보다 큰 묶음을 조각 하나로 세어 개수를 적게 셈`
이라는 같은 산출 경로로 고정했다. 제목과 선택지 수치가 각 출현에서
실제로 맞는지 전용 테스트로 확인한다. SQL도 같은 넓이 비적합 오답과
남은 자리보다 작은 오답을 동시에 요구한다.

세 번째 독립 검증은 12칸을 6개 마름모로 채울 수 없는 합법 입력에서
SQL 재귀 탐색이 중복 배치를 반복해 PostgreSQL 프로세스를 종료하는
차단 결함을 발견했다. exact-cover를 조각별 고유 배치 비트마스크와
상태 중복 제거를 쓰는 반복 DP로 교체하고, 넓이 선검사와 배치·상태
상한을 두었다. 12칸 양성·음성 성능 사례는 1초 제한 pgTAP으로 고정한다.
`security definer`인 오답 근거 검사 함수의 `PUBLIC`, `anon`,
`authenticated` 실행 권한도 회수해 REST RPC로 내부 검수 자료를 확인할
수 없게 했다.

직접 문항의 `2칸`이라는 보이는 수와 `2개` 오답이 겹쳐, 의도한 큰 묶음
오개념 외에 “주어진 수 복사”로도 설명되는 문제는 12칸·마름모 6개
문항으로 다시 설계했다. 오답 `3개`는 4칸짜리 큰 묶음 세 개, `12개`는
단위 삼각형 칸을 그대로 센 결과다. 문맥 숫자와 오답 숫자의 충돌은
전용 하네스와 콘텐츠 검수 스킬 완료 조건에서 차단한다.

전체 시각 하네스는 196문항을 결정적으로 렌더하며 후보 역할을 바꾼
합성 접근성 사례도 검사한다. 보드·후보·기준 조각의 단위 정삼각형은
모두 실제 화면에서 한 변 36px의 같은 축척을 쓴다. 현재 전체 회귀는
Vitest 51파일 418테스트, DB 22파일 365테스트, Playwright 30시나리오,
8개 workspace 타입 검사와 전체 빌드를 통과했다. 다각형 브라우저
회귀는 세 화면 크기에서 10문항을 완주하며 어절 분리·가로 넘침 외에
접근성 설명, 중립색 끝점, 390px 표시 크기, 조각 축척과 빈자리 수
미노출까지 검사한다. 4학년 2학기 콘텐츠와 신규 앵커는 review 상태이며
DB에는 발행하지 않는다.

최종 Kiro CLI Claude Opus 5 검증은 `VERDICT: APPROVE`, 차단·고위험
0건, `NEXT_UNIT_READINESS: READY`를 반환했다. 검증자는 12칸 음성
exact-cover와 혼합 조각 스트레스, DB 재구축, 무작위 TypeScript·SQL
차등 입력 900건, 세 화면 크기와 전체 30개 브라우저 시나리오를 직접
재실행했다. 승인 뒤에도 4-2 선택지 균형, 두 조각 수 영역의 비합동,
TypeScript 12칸·12조각 상한을 자동 회귀로 추가했다.

# A3-6 초4-2 꺾은선그래프 구현 결정

작성일: 2026-08-01
상태: 구현·전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 그래프에는 계산 전 원자료만 둔다

`line-chart-diagram`은 세로축 기준값, 마지막 표시값, 눈금 칸 수,
가로축 항목, 각 점의 눈금 위치만 저장한다. 점의 실제 값 배열,
변화량, 정답 구간, 범례나 점 위 수치 라벨은 두지 않는다. 정답과 두
오답은 이 원자료를 수학 오라클이 다시 계산한다. 화면을 보기만 해도
답이 보이거나 숨은 작성자 메타데이터를 알아야 풀리는 두 극단을 함께
막는다.

## D2. 하나의 그래프 읽기를 다섯 행동으로 분리한다

한 눈금의 값, 한 점의 값, 이웃한 두 시점의 변화량, 가장 많이 증가한
구간, 두 시점 사이 중간값 어림을 별도 단계로 둔다. 각 단계는
direct·transfer 두 문항과 서로 다른 두 오개념을 반복 관찰한다. 교사
신호는 선택형 활동이 그래프 직접 그리기와 자유 설명 능력을 관찰하지
않았음을 명시한다.

## D3. 문항 유형별 필요한 변화 모양만 강제한다

초기 계획의 “모든 계열은 상승과 하락을 모두 포함한다”는 조건은
냉각처럼 자연스럽게 계속 감소하는 자료와 충돌한다. 전역 비단조 조건은
두지 않고, 가장 큰 증가를 묻는 문항에만 양의 증가·고유한 최대 증가·
최고점과의 분리·끝 구간 회피를 요구한다. 변화량 문항은 목표 구간의
고유성과 최소 두 눈금만 검사한다. 이 결정은 진단 지름길을 막으면서도
자료 맥락을 왜곡하지 않는다.

## D4. 시각 접근성도 같은 계산을 요구한다

SVG는 한 계열, 한 점/항목, 축·격자·두 표시 눈금과 필요할 때의
축 생략 물결선만 그린다. 접근성 설명은 각 항목의 `N칸` 원자료를
차례로 읽고 계산된 값·단위·정답·`약` 표현을 말하지 않는다. 390px에서
처음 7px 반지름 점이 실제 12px보다 작게 렌더되는 것을 E2E가 발견해
최종 9px로 키웠다. 1280·768·390·360px와 밝은/어두운 테마에서 실효
점·축 글자 크기 12px, 모든 라벨의 겹침·경계 이탈, 어절 비분리와 가로
넘침 없음까지 검사한다.

## D5. 검수 계약은 런타임과 DB에서 같은 결과를 낸다

TypeScript·Zod·SQL이 눈금 단위, 목표 위치, 변화량 고유성, 최대 증가
지름길과 중간값 조건을 같은 방식으로 거부한다. 결정적 무작위 600건의
TypeScript↔SQL 차등 검사는 불일치 0건이다. 내부 SQL 함수는
`PUBLIC`, `anon`, `authenticated` 실행 권한을 회수했고, 초4-2 세트와
신규 앵커를 DB 발행 원장에 추가하지 않았다.

## D6. 파생 정답도 보이는 점값과 충돌하지 않는다

정답이 수학적으로 맞더라도 변화량이 구간 시작점이나 다른 점의 값과
같으면, 목표 계산을 하지 않고 점 하나만 읽은 학생이 정답 처리된다.
모든 수치형 꺾은선그래프에서 파생 정답을 전체 점값과 대조하고,
점 읽기 문항만 의도한 목표점 자신을 제외한다. 옛 결함 데이터를 되살린
회귀 뮤턴트와 분 단위 숫자 표기 검사, 콘텐츠 검수 스킬·루브릭·정적
감사 규칙을 함께 유지한다.

최종 게이트는 Vitest 53파일 430테스트, DB 23파일 405테스트,
TypeScript↔SQL 600건 불일치 0건, Playwright 31시나리오, 전체 타입
검사와 빌드 통과다. Kiro Opus 5의 콘텐츠·UI·SQL 최종 검증은 모두
`VERDICT: APPROVE`, 차단·고위험 0건을 반환했다. 콘텐츠 체크섬은
`d61e9778e63adf96c492ba8306c7f8b256b15d6d45ba4bbcbe62286647166cb5`다.

# A4-1 초5-1 자연수의 혼합 계산 구현 결정

작성일: 2026-08-01
상태: 구현·전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 단원 정답률보다 계산 순서의 다섯 경계를 본다

곱셈 먼저, 나눗셈 먼저, 같은 순위의 왼쪽부터 계산, 괄호 먼저,
괄호 뒤 전체 순서 적용을 서로 다른 단계로 둔다. 각 단계는 direct와
transfer 두 문항을 가져 한 번의 오답을 부족으로 확정하지 않는다.

## D2. 오답은 실제로 다시 계산되는 두 반복 경로만 쓴다

첫 오답은 앞에서부터 또는 오른쪽부터 잘못 계산한 결과다. 다른 오답은
나누는 수 옆 계산을 먼저 묶거나 첫 중간값을 전체 답으로 적은 결과다.
곱셈 기호를 덧셈으로 바꾸는 식의 학년 부적합한 임의 오답은 제거했다.
독립 재귀 하강 계산기가 10개 정답을 전수 재계산하고, 20개 오답의
선택값·산출식·오개념 반복 횟수를 별도 표와 대조한다. 산출식 안의 모든
등식도 안전한 수식 해석기로 실행해 마지막 값이 실제 선택지와 같은지
확인한다.

## D3. 수식은 의미 단위 전체가 함께 줄을 바꾼다

학생 화면의 띄어 쓴 자연수 사칙연산 식을 하나의
`mom-readable-keep` 그룹으로 묶는다. 1280·768·390·360px의 밝은 화면과
어두운 화면에서 10문항을 모두 진행하며 수식 내부 분리, 선택지 어절
분리, 문서 가로 넘침이 없음을 검사한다.

## D4. 짧은 활동은 정답 위치도 단원 안에서 균형을 맞춘다

기존 독립 해시 셔플은 10문항 가운데 정답 위치 9개가 같은 칸에 놓이는
세션을 만들 수 있었다. 학생 세션별 시작 위치를 해시로 정하고 문항
순서마다 정답 위치를 순환해, 10문항은 항상 3·3·4회로 분배한다.
오답끼리의 순서와 같은 세션의 재현성은 유지하고 실제 제시 ID를 관찰
이벤트에 저장한다.

## D5. 검수본은 발행 원장을 열지 않는다

`[6수01-01]`과 `grade5-semester1`은 TypeScript 교차표와 검수용 데모에만
등록한다. 콘텐츠 상태는 `review`이며 DB 앵커, 세트별 허용 목록,
진단 세트, 교사용 오답 원장은 모두 0행이어야 한다. pgTAP은 미승인
앵커를 사용한 직접 삽입도 발행 정합성 가드가 거부하는지 확인한다.

콘텐츠 체크섬은
`e028838dd6013655b5e31c4b15bdc824ef17a1e30410e04bd807b03c61e7c5f7`다.
최종 게이트는 Vitest 58파일 463테스트, DB 24파일 413테스트,
Playwright 전체 33시나리오와 변경된 초5 표적 2시나리오 재실행, 전체
타입 검사·빌드·런타임 누출 검사를 통과했다. Kiro Opus 5 재검증은
`VERDICT: APPROVE`, blocker·high·medium 0건,
`NEXT_UNIT_READINESS: READY`를 반환했다.

# A4-2 초5-1 약수와 배수 구현 결정

작성일: 2026-08-01
상태: 구현·전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 공식 범위를 두 성취기준과 다섯 관찰 단계로 나눈다

`[6수01-04]`는 약수·공약수·최대공약수, `[6수01-05]`는
배수·공배수·최소공배수를 맡는다. 약수 목록, 공약수와 최대공약수,
배수 목록, 공배수와 최소공배수, 상황 적용의 다섯 단계로 나눈다.
약수와 배수의 첫 단계 사이에는 상류 근거가 없는 선후 관계를 만들지
않고, 상황 적용만 두 공통값 단계를 선수로 요구한다.

## D2. 목록을 직접 찾게 하고 불필요한 그림과 표를 넣지 않는다

약수·배수 목록 자체가 관찰 행동이므로 10문항 모두 `visual:none`으로
둔다. 미리 그린 묶음이나 표가 목록 만들기의 중간 답을 노출하지 않게
한다. 수 목록 선택지는 각 수와 뒤따르는 쉼표를 하나의 읽기 토큰으로
유지하면서 쉼표 뒤 줄바꿈은 허용해 360px에서도 넘치지 않게 한다.

## D3. 두 문항에 같은 두 오해가 되풀이되어야 단계 근거로 쓴다

1과 자기 자신 제외, 나머지 무시, 한쪽 값까지 공통값으로 보기, 공약수 확인 중단,
1배 제외, 1씩 세기, 두 수의 곱부터 공배수 시작, 배수 목록 섞기,
상황 유형 뒤바꾸기, 두 수 더하기를 정확한 선택값으로 재구성한다.
독립 약수·배수 오라클이 10개 정답과 20개 오답을 전수 재생성한다.
계획 초안의 `12+18=20` 오류는 검증 과정에서 발견해 실제 값 `30`으로
고쳤다.

## D4. 학기 세트가 커져도 활동과 정답 위치는 단원 안에서 분리한다

학생 화면은 두 단원을 각각 10문제·약 5분 카드로 만들고 세션 진행률과
완료 상태를 공유하지 않는다. 정답 위치 3·3·4 순환도 단원 안의
`judgmentIndex`를 기준으로 두 단원 각각 검증한다. 교사 화면은 단원별
요약 두 행과 학생별 근거를 제공한다.

## D5. 검수본과 발행 원장의 경계를 유지한다

콘텐츠는 `review` 상태다. `[6수01-04]`, `[6수01-05]`,
`grade5-semester1`과 교사용 오답 근거는 DB에 발행하지 않으며 신규
migration도 만들지 않는다. 별도 pgTAP이 두 앵커의 직접 삽입 거부와
내부 스키마 가드 권한 회수를 확인한다.

공약수 목록·최대공약수와 공배수 목록·최소공배수는 한 보기에 `/`로
합치지 않고 각각 한 행동만 묻도록 분리했다. 대표값 문항의 모든 보기는
문제에 제시한 두 수를 그대로 복사하지 못하게 오라클로 고정했다.

콘텐츠 체크섬은
`64b412abecafd0d5e7641441d124667cc3e4f54c1b2b92f600983fb62cc3093c`,
교차표 digest는
`sha256:0e45d79ee0242e75cbb82c4c5583941c1736abc634fb9e9cd3ca64cc4c4c6559`다.
최종 게이트는 Vitest 59파일 471테스트, DB 25파일 422테스트,
5학년 표적 Playwright 2시나리오(학생 20문항 4폭×2테마와 교사 요약),
전체 타입 검사·빌드·런타임 누출 검사를 통과했다. Kiro Opus 5 최종
재검증은 `VERDICT: APPROVE`, blocker·high·medium 0건,
`NEXT_UNIT_READINESS: READY`를 반환했다.

# A4-3 초5-1 대응 관계 구현 결정

작성일: 2026-08-01
상태: 구현·전체 회귀·Kiro Opus 5 독립 검증 통과

## D1. 두 양의 관계를 다섯 관찰 단계로 나눈다

`[6수02-01]`을 대응표의 새 짝, □·△ 식, 두 양의 계산 방향, 기호식의
역방향 적용, 두 양의 변화량으로 나눈다. 4학년의 한 양 수열과 자연어
라벨 식을 반복하지 않고 완전한 대응표와 기호식으로 확장한다.

## D2. 관계 찾기와 관계 적용을 한 문항에 섞지 않는다

Kiro의 첫 검증에서 생활 맥락 전이 문항이 표에서 관계를 먼저 찾은 뒤
다시 역방향이나 변화량을 계산하도록 되어 진단 원인이 섞인 문제가
발견됐다. 역방향·변화량 네 문항은 □·△가 뜻하는 두 양과 한 연산
관계식을 context에 직접 제시한다. 표 헤더도 추상 기호가 아니라 `묶음 수`,
`구슬 수`처럼 구체적인 양으로 통일해 direct와 transfer 사이에서 표현 층과
문맥을 동시에 바꾸지 않는다.

## D3. 오답은 두 문항에서 같은 계산 경로로 재현한다

마지막 표의 차이를 반복함, 한 양의 증가량을 다른 양에 그대로 더함,
첫 행만 맞는 식, 곱하는 수를 더하는 수로 옮김, □·△ 자리 바꿈,
나눗셈을 곱셈으로 바꿈, 가까운 행에서 한 묶음만 이동함, 반대 계산을
하지 않음, 표에서 본 한 번의 변화량을 그대로 사용함, 변화량과 관계의
수를 더함을 각각 두 문항에서 반복한다. 독립 오라클은 정답 10개와 오답
20개, 원자료 숫자 복사 0건, 정답 크기 순위, 자릿수 근접 오답을 고정한다.

## D4. 대응표는 자료만 읽어 주고 답은 말하지 않는다

모든 문항은 왼쪽 양과 오른쪽 양의 완전한 세 행만 표시한다. HTML 표의
열 머리말, 행 우선 셀 순서, 원자료만 포함한 접근성 설명을 Playwright로
대조한다. 1280·768·390·360px의 밝은/어두운 화면에서 30문항을 완주하며
표의 화면 밖 넘침, 셀 내부 넘침, 12px 미만 글자, 질문·선택지 어절 분리를
차단한다.

## D5. 세 번째 단원도 검수본 경계를 유지한다

`[6수02-01]`과 대응 관계의 교사용 오답 근거는 DB에 발행하지 않는다.
새 migration 없이 별도 pgTAP이 앵커·허용 목록·진단 세트·오답 원장 0행과
직접 삽입 거부, 내부 스키마 가드 권한 회수를 확인한다. 현재 교차표 digest는
`sha256:38b15c93843180fe672d04b1ef64d918a02b15963e7a91bfabf9bc9d878f96b1`,
콘텐츠 체크섬은
`b106efcdf8275b412b6a917fd657d812581ed99e983ead090c030a624d891f6e`다.

# A4-4 초5-1 약분과 통분 구현 결정

`[6수01-06]`, `[6수01-07]`, `[6수01-12]`를 5학년 1학기 4단원
`약분과 통분`에 배치한다. DECK6의 `[6수01-12]` 모듈명은 `분수와 소수의
관계`지만 공식 2026 배치표가 이 성취기준을 4단원에 두므로, 상류 모듈명을
단원 경계로 사용하지 않는다.

진단 구조는 동치분수 곱셈·동치분수 나눗셈·기약분수·통분·이분모 분수
비교·분수와 소수 변환·분수와 소수 비교의 7단계다. 기약분수는 A4-2의
최대공약수, 통분은 A4-2의 최소공배수를 선수로 연결한다. 각 단계는 두
문항과 반복되는 두 오개념을 가지며 총 14문항·28오답 근거다.

기약분수 두 문항의 `한 번만 약분하고 멈춤` 오답은 정답과 같은 유리수인
것이 진단 목표이므로 이 단계에서만 허용한다. 비교 네 문항은 후보 자체를
고르는 문제여서 prompt와 choice 복사를 허용한다. 그 밖의 문항은 같은 값
선택지와 후보 복사를 차단한다. 14문항 모두 수치 행동을 직접 관찰하며,
두 분수막대를 함께 그리면 답을 노출할 수 있어 `visual:none`으로 고정한다.

Kiro 첫 검증에서 `g5s1-frq-10`의 오답 근거가 실제 선택값을 산출하지 못한
결함과 목표 분모 문구만으로 한 오답을 제거할 수 있는 세 문항을 발견했다.
비교 후보를 `2/3, 3/8, 7/10`으로 바꾸어 분자만 보는 오류와 분모만 보는
오류를 분리하고, 목표 분모가 있는 약분 문항은 세 선택지 분모를 모두
지정값으로 맞췄다. 오개념 제목도 direct·transfer 양쪽을 설명하도록
방향 중립적으로 고쳤다. 독립 오라클은 정답뿐 아니라 28개 오답도 산출
함수로 재생성하며 목표 분모, 유리수 동치 예외, 후보 복사 예외를 검사한다.

보완본은 네 단원·22단계·44판단, 교육과정 앵커 7개, 단원 카드 4개이며
4단원은 14문제·약 7분이다. crosswalk revision은 `2026-08-01.4`, digest는
`sha256:195aeddd0f7bbcbb8480a6478782c383b1cb610fb8eb177fadeb6990a3702c52`,
coverage revision은 `2026-08-01.5`, canonical checksum은
`b27fb6571b22adc15cc7733b7fdc9fc0a808fd1fb094420311303017d392a1c8`이다.
Kiro 재검증은 첫 high 1건, medium 3건, low 4건이 모두 닫혔음을 확인하고
`VERDICT: APPROVE`, blocker·high·medium 0건,
`NEXT_UNIT_READINESS: READY`를 반환했다. 이후 단원부터는 단원 작업 중
변경 범위 테스트와 학생·교사 대표 화면만 검사하고, 전체 DB·전체
Playwright·전체 문서 정리는 학기 완료 게이트에서 한 번 수행한다.

최종 게이트는 Vitest 60파일 484테스트, DB 26파일 430테스트, 전체 타입
검사·빌드·교차표 재현성·79파일 콘텐츠 감사와 5학년 표적 Playwright
2시나리오를 통과했다. Kiro Opus 5 재검증은 `VERDICT: APPROVE`,
blocker·high·medium 0건, `NEXT_UNIT_READINESS: READY`를 반환했다.
임의의 두 표 셀을 계산해 우연히 정답과 같아지는 경우는 정상 대응표에도
빈번해 기계 차단하지 않고, 학생이 그 셀 조합을 고를 실제 시각·수학적
근거가 있는지를 콘텐츠 검수 스킬의 사람 검토 항목으로 남긴다.

# A4 검증 실행 주체 전환과 A4-5 완료 결정

2026-08-01부터 Kiro를 계획·구현·검증 실행 경로에서 제외한다. 이후 단원은
Codex가 자체적으로 계획하고 구현한 뒤, 전용 Codex 검증 서브에이전트가
변경 범위를 읽기 전용으로 검증한다. 결함이 있으면 Codex가 수정하고 같은
서브에이전트가 정확히 그 보완 범위를 재검증한다. 매 단원마다 전체 검사를
반복하지 않고 변경 범위 검사와 대표 실화면 검사를 수행하며, 학기 전체
회귀는 학기 완료 게이트에서 수행한다.

A4-5 `분수의 덧셈과 뺄셈`은 6단계·12문항으로 완료했다. 학생·교사·스튜디오
표시의 슬래시 분수는 원문 데이터는 유지하되 분자·가로선·분모가 쌓인 교과서
분수로 렌더한다. `2 1/3과`와 `1과 2/7 + 2와 3/7을` 같은 대분수 표현은
하나의 줄바꿈 단위로 유지한다. A4-5 오개념 ID는 선택지 A/B 위치가 아니라
수학적 오류의 의미로 기록한다. 집중 검사 4파일·59테스트, 타입 검사, 빌드와
390×844 실화면 검사를 통과했고, Codex 검증 서브에이전트 재검증은
blocker·high·medium·low 모두 0으로 `APPROVE`했다.

# A4-6 초5-1 다각형의 둘레와 넓이 구현 결정

`[6수03-11]`~`[6수03-14]`를 5학년 1학기 6단원에 배치한다. 진단 구조는
둘레, 넓이 단위, 직사각형·정사각형, 평행사변형, 삼각형, 사다리꼴,
마름모의 7단계이며 각 단계는 direct·transfer 두 문항을 가진다. 모든
계산 오답은 둘레와 넓이 혼동, 밑변과 높이 더하기, 2로 나누기 누락처럼
실제 계산 경로로 재생성한다. 교실과 공책의 넓이 단위 문항은 수치 계산과
분리해 단위 감각만 관찰한다.

한 `perimeter-area-diagram` 계약 안에서 도형별 허용 필드를 엄격히 나눈다.
TypeScript discriminated union, Zod의 shape별 exact-field 검사, PostgreSQL
JSONB validator가 같은 계약을 공유하며 내부 validator와 runtime assertion은
PUBLIC·anon·authenticated 실행 권한을 회수한다. 그림 데이터에는 정답이나
넓이 값을 저장하지 않는다.

표시된 길이와 도형 모양이 충돌하면 수학이 아닌 그림 읽기 오류를 만들기
때문에, 한 그림 안의 모든 길이는 동일 축척으로 계산한다. 직사각형의 두 변,
평행사변형·삼각형의 밑변과 높이, 사다리꼴의 두 밑변과 높이, 마름모의 두
대각선 비례를 렌더러 회귀 검사로 고정한다. 실브라우저 점검에서 SVG가
존재하지 않는 색상 토큰을 참조해 윤곽선과 높이선이 흐려진 결함을 발견해
`--text-primary`, `--text-secondary`, `--accent-primary`로 교체했다.

첫 Codex 독립 검증은 현재 값은 맞지만 오답 선택값과 산출식을 직접 연결하는
오라클, 활성 도형 전체의 좌표 비례 회귀, 내부 SQL 함수의 역할별 권한 검사가
부족하다고 판정했다. 28개 오답을 의미 ID별 식으로 다시 계산하고 12개 도형의
축척·수직 높이·직각·대각선을 좌표로 검사하며, 내부 함수 5개 모두의
`anon`·`authenticated` 실행 차단을 고정했다. 보완 재검증은 전 등급 0건,
`VERDICT: APPROVE`로 닫혔다. canonical checksum은
`7b63014d3e730a361bc3ed62109baeccfa0812231093d74537280b499216bc87`,
crosswalk digest는
`sha256:595d0d20d88e417cd41b84650b041318ceb4dc9e1f102c3751c5f50948f3b34b`다.

# 5학년 2학기~6학년 2학기 연속 구현 결정

공식 2026 성취기준-단원 배치표를 기준으로 5학년 2학기 6단원
33단계·66문항, 6학년 1학기 6단원 31단계·62문항, 6학년 2학기 6단원
28단계·56문항을 구현한다. 총 신규 분량은 18단원·92단계·184문항이다.
각 단계는 direct·transfer 두 문항과 두 반복 오개념을 유지한다.

DECK6 고정 스냅숏은 5~6학년군 전체 45개 성취기준·135개 세부 주제·
33개 상류 선수 후보로 확장한다. 학기 배치는 공식 경기도교육청 자료가
결정하고, DECK6는 코드·영역·주제·선수 후보 대조에만 쓴다.

공용 시각자료에는 입체 구조·전개도·치수·쌓기나무, 띠그래프·원그래프,
다중 90도 회전을 추가한다. 점대칭은 정확한 180도 변환으로 계산하고,
쌓기나무는 중복·공중 블록을 스키마와 SQL에서 거부한다. 원 그림은
반지름 값과 지름 값을 별도 필드로 구분해 `지름 20 m`를 반지름 구간에
붙이는 오류를 차단한다. 분수 원문은 슬래시로 저장하되 모든 학생 표시에서
쌓인 교과서 분수로 렌더한다.

검증은 학기마다 전체 저장소를 반복하지 않는다. 각 학기의 단계 수·문항 수·
스키마·커버리지·체크섬을 전수 검사하고, 새 의미 시각자료만 각각 한 번
렌더링한다. 세 학기 조립 후 전체 단위 테스트, 타입 검사, 빌드, DB,
교차표 재현성, 콘텐츠 감사와 대표 브라우저 화면을 한 번 실행한다.
