name: Purge expired pilots

on:
  schedule:
    - cron: "17 18 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pilot-retention-purge
  cancel-in-progress: false

jobs:
  purge:
    runs-on: ubuntu-latest
    environment: ${{ matrix.environment }}
    strategy:
      max-parallel: 1
      matrix:
        # 운영 Supabase 프로젝트를 만들면 production-retention 을 다시 넣는다.
        # 지금은 그 Environment 에 URL·service-role key 가 없어 매일 밤 실패만
        # 하고, 삭제 대상 데이터도 없다.
        environment: [staging-retention]
    steps:
      - name: Run the service-role-only retention boundary
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          test -n "$SUPABASE_URL"
          test -n "$SUPABASE_SERVICE_ROLE_KEY"
          curl --fail-with-body --silent --show-error \
            --request POST "$SUPABASE_URL/rest/v1/rpc/purge_expired_pilot_data" \
            --header "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            --header "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            --header "Content-Type: application/json" \
            --data '{}'
