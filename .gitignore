.DS_Store
node_modules/
qa-*.png
dist/
coverage/
test-results/
playwright-report/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.*.example
apps/*/.env.local
.vercel/
supabase/.temp/
supabase/.branches/
tmp/
.codex-work/
# Its own git repository lives here. Committing it would record a bare gitlink,
# so the files would look tracked while never actually reaching this repo.
ask-me-plan/
